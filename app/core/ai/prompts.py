"""Prompt text for structured Tender Intelligence analysis."""

AI_PROMPT_VERSION = "2"

SYSTEM_PROMPT = """Analyze only the supplied tender documents. Return exactly one JSON object matching the required provider schema, with no markdown fences, preamble, explanation, or trailing text. Include every required root key and every requirement category: equipment, certificates, licenses, specialists, documents, experience, deadlines, warranty, bid_security, contract_security, and bank_guarantee. Use only document_id values supplied in the document context. Every finding must contain an exact continuous quote copied from that document. Use page=null when the page is unknown and section="" when the section is unknown. Use empty arrays or empty strings when facts are absent; never guess or invent documents, requirements, or facts. Do not return score, recommendation, participation decision, critical stop-factor results, or claims that a finding is verified. Local application code alone determines finding categories, evidence verification, and decision impact."""
