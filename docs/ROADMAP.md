# Дорожная карта CorterisTenderAI v2

## Текущее состояние
- RM-001–RM-107: `DONE`.
- RM-108: `DONE`.
- RM-109: `DONE`.
- RM-110: `DONE`.
- RM-111: `DONE`.
- RM-112: `DONE`.
- RM-113: `DONE`.
- RM-114: `DONE`.
- RM-115: `DONE`.
- RM-116: `DONE`.
- RM-117: `DONE`.
- RM-118: `DONE`.
- RM-119: `DONE`.
- RM-120: `DONE`.
- RM-121: `DONE`.
- RM-122: `DONE`.
- RM-123: `DONE`.
- RM-124: `DONE`.
- RM-125: `DONE`.
- RM-126: `DONE` (включая `RM-126.1`).
- RM-127: `DONE`.
- RM-128: `DONE`.
- RM-129: `DONE`.
- RM-130: `DONE`.
- RM-131: `DONE`.
- RM-132: `DONE`.
- RM-133: `DONE`.
- RM-134: `DONE`.
- RM-135: `DONE`.
- RM-136: `DONE`.
- RM-137: `DONE`.
- RM-138: `DONE`.
- RM-139: `DONE`.
- RM-140: `DONE`.
- RM-141: `DONE`.
- RM-142: `DONE`.
- RM-143: `DONE`.
- RM-144: `DONE`.
- RM-145: `DONE`.
- RM-146: `DONE`.
- RM-147: `DONE`.
- RM-148: `DONE`.
- RM-149: `DONE`.
- RM-150: `DONE`.
- RM-151: `DONE`.
- RM-152: `DONE`.
- RM-153: `DONE`.
- RM-154: `DONE`.
- RM-155: `DONE`.
- RM-156: `IN PROGRESS`.
- RM-157–RM-200: `PLANNED`.

## Правила
1. Нумерация RM-001–RM-200 неизменна.
2. Перед каждым RM проводится аудит.
3. Переход возможен только после Definition of Done.
4. Один источник — один адаптер.
5. AI не заменяет детерминированную логику.
6. Критический стоп-фактор имеет приоритет.

- [x] RM-001 — Завершённый этап проекта.
- [x] RM-002 — Завершённый этап проекта.
- [x] RM-003 — Завершённый этап проекта.
- [x] RM-004 — Завершённый этап проекта.
- [x] RM-005 — Завершённый этап проекта.
- [x] RM-006 — Завершённый этап проекта.
- [x] RM-007 — Завершённый этап проекта.
- [x] RM-008 — Завершённый этап проекта.
- [x] RM-009 — Завершённый этап проекта.
- [x] RM-010 — Завершённый этап проекта.
- [x] RM-011 — Завершённый этап проекта.
- [x] RM-012 — Завершённый этап проекта.
- [x] RM-013 — Завершённый этап проекта.
- [x] RM-014 — Завершённый этап проекта.
- [x] RM-015 — Завершённый этап проекта.
- [x] RM-016 — Завершённый этап проекта.
- [x] RM-017 — Завершённый этап проекта.
- [x] RM-018 — Завершённый этап проекта.
- [x] RM-019 — Завершённый этап проекта.
- [x] RM-020 — Завершённый этап проекта.
- [x] RM-021 — Завершённый этап проекта.
- [x] RM-022 — Завершённый этап проекта.
- [x] RM-023 — Завершённый этап проекта.
- [x] RM-024 — Завершённый этап проекта.
- [x] RM-025 — Завершённый этап проекта.
- [x] RM-026 — Завершённый этап проекта.
- [x] RM-027 — Завершённый этап проекта.
- [x] RM-028 — Завершённый этап проекта.
- [x] RM-029 — Завершённый этап проекта.
- [x] RM-030 — Завершённый этап проекта.
- [x] RM-031 — Завершённый этап проекта.
- [x] RM-032 — Завершённый этап проекта.
- [x] RM-033 — Завершённый этап проекта.
- [x] RM-034 — Завершённый этап проекта.
- [x] RM-035 — Завершённый этап проекта.
- [x] RM-036 — Завершённый этап проекта.
- [x] RM-037 — Завершённый этап проекта.
- [x] RM-038 — Завершённый этап проекта.
- [x] RM-039 — Завершённый этап проекта.
- [x] RM-040 — Завершённый этап проекта.
- [x] RM-041 — Завершённый этап проекта.
- [x] RM-042 — Завершённый этап проекта.
- [x] RM-043 — Завершённый этап проекта.
- [x] RM-044 — Завершённый этап проекта.
- [x] RM-045 — Завершённый этап проекта.
- [x] RM-046 — Завершённый этап проекта.
- [x] RM-047 — Завершённый этап проекта.
- [x] RM-048 — Завершённый этап проекта.
- [x] RM-049 — Завершённый этап проекта.
- [x] RM-050 — Завершённый этап проекта.
- [x] RM-051 — Завершённый этап проекта.
- [x] RM-052 — Завершённый этап проекта.
- [x] RM-053 — Завершённый этап проекта.
- [x] RM-054 — Завершённый этап проекта.
- [x] RM-055 — Завершённый этап проекта.
- [x] RM-056 — Завершённый этап проекта.
- [x] RM-057 — Завершённый этап проекта.
- [x] RM-058 — Завершённый этап проекта.
- [x] RM-059 — Завершённый этап проекта.
- [x] RM-060 — Завершённый этап проекта.
- [x] RM-061 — Завершённый этап проекта.
- [x] RM-062 — Завершённый этап проекта.
- [x] RM-063 — Завершённый этап проекта.
- [x] RM-064 — Завершённый этап проекта.
- [x] RM-065 — Завершённый этап проекта.
- [x] RM-066 — Завершённый этап проекта.
- [x] RM-067 — Завершённый этап проекта.
- [x] RM-068 — Завершённый этап проекта.
- [x] RM-069 — Завершённый этап проекта.
- [x] RM-070 — Завершённый этап проекта.
- [x] RM-071 — Завершённый этап проекта.
- [x] RM-072 — Завершённый этап проекта.
- [x] RM-073 — Завершённый этап проекта.
- [x] RM-074 — Завершённый этап проекта.
- [x] RM-075 — Завершённый этап проекта.
- [x] RM-076 — Завершённый этап проекта.
- [x] RM-077 — Завершённый этап проекта.
- [x] RM-078 — Завершённый этап проекта.
- [x] RM-079 — Завершённый этап проекта.
- [x] RM-080 — Завершённый этап проекта.
- [x] RM-081 — Завершённый этап проекта.
- [x] RM-082 — Завершённый этап проекта.
- [x] RM-083 — Завершённый этап проекта.
- [x] RM-084 — Завершённый этап проекта.
- [x] RM-085 — Завершённый этап проекта.
- [x] RM-086 — Завершённый этап проекта.
- [x] RM-087 — Завершённый этап проекта.
- [x] RM-088 — Завершённый этап проекта.
- [x] RM-089 — Завершённый этап проекта.
- [x] RM-090 — Завершённый этап проекта.
- [x] RM-091 — Завершённый этап проекта.
- [x] RM-092 — Завершённый этап проекта.
- [x] RM-093 — Завершённый этап проекта.
- [x] RM-094 — Завершённый этап проекта.
- [x] RM-095 — Завершённый этап проекта.
- [x] RM-096 — Завершённый этап проекта.
- [x] RM-097 — Завершённый этап проекта.
- [x] RM-098 — Завершённый этап проекта.
- [x] RM-099 — Завершённый этап проекта.
- [x] RM-100 — Завершённый этап проекта.
- [x] RM-101 — Завершённый этап проекта.
- [x] RM-102 — Завершённый этап проекта.
- [x] RM-103 — Завершённый этап проекта.
- [x] RM-104 — Завершённый этап проекта.
- [x] RM-105 — Завершённый этап проекта.
- [x] RM-106 — Завершённый этап проекта.
- [x] RM-107 — Единая объяснимая оценка участия, рекомендация и план действий.

# RM-108–RM-125 — резюме и AI
- [x] RM-108 — AI-резюме тендера.
- [x] RM-109 — AI-анализ тендерной документации.
- [x] RM-110 — стабилизация Tender Intelligence.
- [x] RM-111 — AI Orchestrator.
- [x] RM-112 — выбор AI-провайдера.
- [x] RM-113 — локальный режим.
- [x] RM-114 — OpenAI-совместимый API.
- [x] RM-115 — строгая JSON-схема.
- [x] RM-116 — ссылки, цитаты и provenance.
- [x] RM-117 — анализ ТЗ.
- [x] RM-118 — анализ проекта договора в тендере.
- [x] RM-119 — анализ требований к заявке.
- [x] RM-120 — юридические риски.
- [x] RM-121 — финансовые риски.
- [x] RM-122 — анализ конкуренции.
- [x] RM-123 — полнота документации.
- [x] RM-124 — повторная проверка AI.
- [x] RM-125 — стабилизация AI-платформы.

## Обязательный prerequisite RM-111 — воспроизводимый quality gate

Статус prerequisite: `DONE` (PR #22, merge `ebfdf01`). Обе обязательные
Windows-проверки на Python 3.12 и 3.13 прошли на PR и повторно на `main`;
branch protection требует PR, актуальную ветку и оба check context.

До реализации бизнес-логики AI Orchestrator в рамках RM-111 необходимо:

- изолировать offline-тесты от Windows Credential Manager и реальных API;
- исключить сетевой запрос при запуске тестов без явно разрешённого live-режима;
- добавить воспроизводимый Windows quality gate для Python 3.12 и 3.13;
- включить в обязательный gate полный pytest, Ruff check, Ruff format check и
  зафиксированный mypy-контур;
- сохранить C19 live-проверку отдельной ручной/разрешённой операцией;
- не изменять в этом пакете AI-бизнес-логику, C17 или C19.

Prerequisite считается выполненным только после прохождения полного baseline в
обычном пользовательском окружении, включая окружение с сохранёнными credentials,
без чтения этих credentials и без сетевых обращений из offline-тестов.

## RM-111 — implementation acceptance

Статус реализации: `DONE` (PR #24, merge `f246381`).

- создан единый `TenderAiOrchestrator` поверх существующего task-service;
- полный анализ больше не вызывает AI task-service или provider напрямую;
- текущий AI-результат явно передаётся в RM-107;
- status-to-warning policy и последняя exception boundary централизованы;
- production runtime создаёт один Orchestrator и один общий AI repository;
- legacy `structured_analysis` больше не является provider workflow и не
  рассчитывает собственные score/recommendation;
- UI показывает отдельную стадию, существующий HTML/JSON payload сохранён;
- миграция БД не требуется;
- целевой набор: `93 passed`; полный локальный регресс: `748 passed`;
- Ruff check, Ruff format check и mypy для 7 файлов проходят.

PR #24 слит в `main` 13 июля 2026 года. Quality Gate merge-коммита
`f246381` успешно прошёл на Python 3.12 и 3.13. RM-111 соответствует
Definition of Done; следующим активным этапом назначен RM-112.

## RM-112 — implementation acceptance

Статус реализации: `DONE` (PR #26, merge `1d559b5`).

- канонический persisted source — секция `ai` существующего `ConfigManager`;
- stable provider IDs: `disabled`, `openai`, `openai_compatible`;
- переиспользованы `DisabledProvider` и единый `OpenAICompatibleProvider`;
- default, unknown/corrupt config и ошибки credential storage дают безопасный
  `disabled` fallback без чтения keyring в disabled flow и без сетевого вызова;
- legacy display label не активирует provider, non-secret draft migration
  идемпотентна;
- выбранный provider внедряется bootstrap в существующий runtime с одним
  analyzer, Orchestrator и repository;
- переиспользована существующая ChatGPT/ИИ вкладка, secret не хранится в UI
  или config, сохранение не выполняет connection test;
- local/Ollama не входил в RM-112 и был отложен до отдельного RM-113;
- миграция БД не требуется;
- целевой набор: `62 passed`; полный локальный регресс: `784 passed` за 52,92 с;
- Ruff check/format, mypy для 9 файлов, secret scan, dependency audit и
  `git diff --check` проходят.

PR #26 слит в `main` 13 июля 2026 года коммитом `1d559b5`. Post-merge Quality
Gate успешен на Python 3.12 и 3.13. После этого следующим активным этапом был назначен
RM-113; его application-код требовал отдельного аудита.

## RM-113 — implementation acceptance

Статус реализации: `DONE` (PR #28, merge `ef8b296`).

- добавлен stable provider ID `ollama` и UI-label «Ollama — локально»;
- local endpoint ограничен `localhost`, `127.0.0.0/8` и `::1`, URL нормализуется к `/v1`;
- переиспользован единый `OpenAICompatibleProvider` без отдельного HTTP adapter;
- Ollama не требует cloud API key и не обращается к keyring;
- resolve, bootstrap, load и save не выполняют сеть или health-check;
- недоступный Ollama безопасно возвращает `provider_error`, а детерминированный анализ
  продолжает работу;
- сохранены единые `TenderDocumentAiAnalyzer`, `TenderAiOrchestrator` и AI repository;
- RM-107 остаётся владельцем score/recommendation и приоритета critical stop-factor;
- новая БД или миграция БД не требуются;
- целевой набор: `58 passed`; полный локальный регресс: `808 passed`;
- Ruff check/format, mypy, secret scan, dependency audit и `git diff --check` проходят.

PR #28 слит в `main` 14 июля 2026 года коммитом `ef8b296`. Post-merge Quality Gate
run `29285835443` успешно прошёл на Python 3.12 и 3.13. RM-113 соответствует Definition
of Done; следующим активным этапом назначен RM-114.

## RM-114 — implementation acceptance

Статус реализации: `DONE` (PR #30, merge `e4caca0`).

- укреплён существующий `OpenAICompatibleProvider` без второго HTTP adapter или AI pipeline;
- закреплён единый синхронный `POST /responses` contract для OpenAI,
  `openai_compatible` и Ollama request profile;
- redirects запрещены, response ограничен 4 MiB, timeout валидируется, а HTTP/JSON/network/
  TLS/refusal/incomplete ошибки возвращаются только как безопасные stable codes;
- cloud request использует `store=false`, Ollama сохраняет подтверждённый compatibility subset;
- generic URL и credential boundaries исключают query/fragment/userinfo/control characters и
  небезопасный Authorization header;
- сохранены единый analyzer/Orchestrator/repository, существующий bootstrap DI, RM-107
  score/recommendation и абсолютный приоритет critical stop-factor;
- новая БД или миграция БД не требуются;
- локальная приёмка: целевой набор `152 passed`, полный pytest `863 passed`, Ruff
  check/format, mypy (10 файлов), secret scan, dependency audit и `git diff --check` успешны.

PR #30 слит в `main` 14 июля 2026 года коммитом `e4caca0`. Post-merge Quality Gate
run `29315630189` успешно прошёл на Python 3.12 (`863 passed`) и Python 3.13
(`863 passed`). RM-114 соответствует Definition of Done; следующим активным этапом
назначен RM-115, реализация которого требует отдельного аудита.

## RM-115 — implementation acceptance

Статус реализации: `DONE` (PR #33, merge `f2573c4`).

- введена единая Pydantic v2 provider-output схема с детерминированным JSON Schema и
  строгим fail-closed decoder без частичного принятия структурно невалидных payload;
- OpenAI и generic `openai_compatible` используют Responses API `text.format`, Ollama
  сохраняет подтверждённый compatibility subset без второго запроса или downgrade;
- существующие provider/analyzer/Orchestrator/repository и production DI переиспользованы;
- evidence подтверждается только точным локальным совпадением цитаты с документом;
- RM-107 остаётся единственным владельцем score/recommendation и абсолютного приоритета
  critical stop-factor;
- persisted schema остаётся версии 2; новая БД или миграция БД не требуются;
- локальная приёмка: целевой набор `229 passed`, полный pytest `901 passed`, Ruff
  check/format, mypy (13 файлов), secret scan, dependency audit и `git diff --check` успешны.

PR #33 слит в `main` 14 июля 2026 года коммитом `f2573c4`. Post-merge Quality Gate
run `29352442656` после повторного запуска успешно прошёл на Python 3.12
(`901 passed in 74.68s`) и Python 3.13 (`901 passed in 77.29s`). Первый Python 3.12 job
завершился нативным Windows access violation внутри pytest; повторный job того же merge SHA
прошёл полностью. RM-115 соответствует Definition of Done; следующим активным этапом
назначен RM-116, application-код которого требует отдельного аудита.

## RM-116 — implementation acceptance

Статус реализации: `DONE` (PR #35, merge `b8ff9b1`).

- введены локально проверяемые exact citations, source registry и immutable provenance;
- persisted payload повышен до версии 3, legacy/future/corrupt cache обрабатывается fail-closed
  без миграции существующей SQLite-таблицы;
- безопасные citation links используют только внутреннюю схему и существующий documents dialog;
- JSON/HTML export сохраняет provenance и использует только escaped internal anchors;
- RM-107 учитывает только current verified citations, не меняя score/recommendation или
  абсолютный приоритет critical stop-factor;
- сохранены существующие provider/analyzer/Orchestrator/repository/context builder/exporter,
  одна provider-output схема и один production provider call;
- локальная приёмка на Python 3.12.7: target `273 passed`, provider/UI regressions `97 passed`,
  full `1029 passed`, Ruff, mypy (16 файлов), secret scan, dependency audit и diff-check успешны.

PR #35 слит в `main` 15 июля 2026 года коммитом `b8ff9b1`. Post-merge Quality Gate run
`29372896780` успешно прошёл на Python 3.12 (`1030 passed in 123.59s`) и Python 3.13
(`1030 passed in 60.47s`); Ruff, mypy (16 файлов), secret scan, smoke tests и dependency
audit успешны на обеих версиях. RM-116 соответствует Definition of Done; следующим активным
этапом назначен RM-117, application-код которого требует отдельного аудита.

## RM-117 — implementation acceptance

Статус реализации: `DONE` (PR #37, merge `c9d5a31`).

- детерминированная классификация ТЗ переиспользуется AI context builder без второй таблицы
  правил; TS documents получают стабильный приоритет и completeness-aware fingerprint;
- строгая provider-output schema v2 добавляет обязательный раздел из 13 групп без local
  status/category/verified/decision fields;
- persisted payload повышен до версии 4, semantic document kind входит в current provenance,
  legacy/future/corrupt cache обрабатывается fail-closed без миграции SQLite;
- специализированные TS findings подтверждаются только единым RM-116 citation resolver и
  только для локально известных TS documents; неподтверждённые findings не влияют на RM-107;
- existing analyzer/service/Orchestrator/repository/UI/export и один provider call сохранены;
  новая AI-stage, TS service/repository/table или UI workflow не созданы;
- локальная приёмка на Python 3.12.7: target `214 passed`, full `1043 passed`, Ruff,
  mypy (16 файлов), secret scan, dependency audit и diff-check успешны.

PR #37 слит в `main` 15 июля 2026 года коммитом `c9d5a31`. Post-merge Quality Gate run
`29376283665` успешно прошёл на Python 3.12 (`1043 passed in 100.66s`) и Python 3.13
(`1043 passed in 150.13s`); Ruff (`509 files`), mypy (16 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих
версиях. RM-117 соответствует Definition of Done; следующим активным этапом назначен RM-118,
application-код которого требует отдельного аудита.

## RM-118 — implementation acceptance

Статус реализации: `DONE` (PR #39, merge `40b7da2`).

- единый public classifier надёжно выделяет `DocumentKind.DRAFT_CONTRACT`, сохраняет приоритет
  ТЗ и переиспользуется deterministic analyzer и AI context builder;
- draft-contract completeness metadata входит в fingerprint, а порядок контекста остаётся
  стабильным: ТЗ, проект договора, извещение/смета, остальные документы;
- строгая provider-output schema v3 добавляет обязательный раздел из 16 групп без local
  status/category/verified/legal-risk/financial-risk/decision fields;
- persisted payload повышен до версии 5; legacy читается безопасно, future/corrupt cache и
  неподходящее evidence обрабатываются fail-closed без миграции SQLite;
- специализированные contract findings подтверждаются единым RM-116 citation resolver только
  для current locally classified contract documents и не входят в RM-107 evidence/actions;
- existing analyzer/service/Orchestrator/repository/UI/export и один provider call сохранены;
  новая AI-stage, contract service/repository/table или UI workflow не созданы;
- локальная приёмка на Python 3.12.7: target `277 passed in 11.82s`, full
  `1080 passed in 51.12s`, Ruff (`510 files`), mypy (16 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #39 слит в `main` 15 июля 2026 года коммитом `40b7da2`. Post-merge Quality Gate run
`29399058186` успешно прошёл на Python 3.12 (`1080 passed in 79.65s`) и Python 3.13
(`1080 passed in 57.69s`); Ruff (`510 files`), mypy (16 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих
версиях. RM-107 score/recommendation и абсолютный приоритет critical stop-factor не изменены;
новая БД или миграция БД не потребовались. RM-118 соответствует Definition of Done; следующим
активным этапом назначен RM-119, application-код которого требует отдельного аудита.

## RM-119 — implementation acceptance

Статус реализации: `DONE` (PR #41, merge `dedc361`).

- единый public classifier выделяет application requirements, application form, instructions и
  procurement notice, сохраняя приоритет ТЗ и проекта договора; AI context builder переиспользует
  ту же каноническую область источников;
- application completeness metadata входит в fingerprint, а scoped context формируется в
  стабильном порядке и безопасно отмечает неполный либо дублированный комплект;
- строгая provider-output schema v4 добавляет обязательный раздел из 21 группы без local
  status/category/verified/legal-risk/financial-risk/decision fields;
- persisted payload повышен до версии 6; legacy v1–v5 читается как unverified/unavailable,
  future/corrupt cache и неподходящее evidence обрабатываются fail-closed без миграции SQLite;
- application findings подтверждаются единым RM-116 citation resolver только для current locally
  classified application documents и не входят в RM-107 evidence/actions;
- existing analyzer/service/Orchestrator/repository/UI/export и один provider call сохранены;
  новая AI-stage, application service/repository/table или UI workflow не созданы;
- локальная приёмка на Python 3.12.7: target `311 passed in 12.91s`, full
  `1114 passed in 55.17s`, Ruff (`511 files`), mypy (16 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #41 слит в `main` 15 июля 2026 года коммитом `dedc361`. Post-merge Quality Gate run
`29406013475` успешно прошёл на Python 3.12 (`1114 passed in 88.59s`) и Python 3.13
(`1114 passed in 77.06s`); Ruff (`511 files`), mypy (16 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих
версиях. RM-107 score/recommendation и абсолютный приоритет critical stop-factor не изменены;
новая БД или миграция БД не потребовались. RM-119 соответствует Definition of Done; следующим
активным этапом назначен RM-120, application-код которого требует отдельного аудита.

## RM-120 — implementation acceptance

Статус реализации: `DONE` (PR #43, merge `f2f87ff`).

- локальный versioned legal-review registry строится только из current verified specialized
  requirements, technical specification и draft-contract findings;
- category, review priority, stable risk ID и recommended action задаются фиксированной pure
  policy без второго AI-вызова, text heuristics или сетевой юридической проверки;
- generic root risks и deterministic stop-factors не копируются в legal registry;
- persisted payload повышен до версии 7, analyzer — до версии 8; provider schema/format v4,
  prompt v6, context v5 и citation resolver v1 не изменены;
- current v7 cache локально пересчитывается и сверяется, legacy v1–v6 остаётся unavailable,
  future/corrupt/tampered data обрабатывается fail-closed без новой таблицы или миграции SQLite;
- existing AI tab и JSON/HTML exporter показывают четыре статуса, priorities, actions, current
  citations, warnings и disclaimer с HTML escaping;
- RM-107 score/recommendation/actions/evidence и абсолютный приоритет critical stop-factor не
  изменены;
- локальная приёмка на Python 3.12.7: target `342 passed in 11.92s`, full
  `1198 passed in 51.00s`, Ruff (`513 files`), mypy (17 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #43 слит в `main` 15 июля 2026 года коммитом `f2f87ff`. Post-merge Quality Gate run
`29411717306` успешно прошёл на Python 3.12 (`1198 passed in 74.13s`) и Python 3.13
(`1198 passed in 73.26s`); Ruff (`513 files`), mypy (17 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих версиях.
Новая AI-stage, provider call, repository, БД или миграция не добавлены. RM-120 соответствует
Definition of Done; следующим активным этапом назначен RM-121, application-код которого требует
отдельного аудита финансового анализа.

## RM-121 — implementation acceptance

Статус реализации: `DONE` (PR #45, merge `ac1cec2`).

- pure local financial-condition registry строится только из current verified specialized
  requirements, technical specification и draft-contract findings;
- category, review priority, stable risk ID, title и recommended action задаются фиксированной
  policy без второго AI-вызова, text heuristics, money parsing или финансового прогноза;
- generic root findings и deterministic stop-factors не копируются в financial registry;
- persisted payload повышен до версии 8, analyzer — до версии 9; provider schema/format v4,
  prompt v6, context v5, citation resolver v1 и legal policy v1 не изменены;
- current v8 cache локально пересчитывается и сверяется, legacy v1–v7 остаётся unavailable,
  future/corrupt/tampered data обрабатывается fail-closed без новой таблицы или миграции SQLite;
- existing AI tab и JSON/HTML exporter показывают четыре статуса, priorities, actions, current
  citations, warnings и финансовый disclaimer с HTML escaping;
- RM-107 score/recommendation/actions/evidence и абсолютный приоритет critical stop-factor не
  изменены; CommercialEstimator сохраняет Decimal-границу и `DATA_INSUFFICIENT` без вымышленных
  total cost, profit или margin;
- локальная приёмка на Python 3.12.7: target `337 passed in 13.12s`, full
  `1289 passed in 55.25s`, Ruff (`515 files`), mypy (18 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #45 слит в `main` 15 июля 2026 года коммитом `ac1cec2`. Post-merge Quality Gate run
`29416563733` успешно прошёл на Python 3.12 (`1289 passed in 66.64s`) и Python 3.13
(`1289 passed in 88.80s`); Ruff (`515 files`), mypy (18 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих версиях.
Новая AI-stage, provider call, repository, calculator, БД или миграция не добавлены. RM-121
соответствует Definition of Done; следующим активным этапом назначен RM-122, application-код
которого требует отдельного аудита анализа конкуренции.

## RM-122 — implementation acceptance

Статус реализации: `DONE` (PR #47, merge `4ebbf6c`).

- pure local competition-condition registry строится только из current verified specialized
  requirements, technical specification и draft-contract findings;
- category, review priority, stable condition ID, title и recommended action задаются
  фиксированной policy без второго AI-вызова, statement matching, regex или market prediction;
- generic root findings, deterministic stop-factors, legacy `COMP_RULES`, `raw_metadata`,
  commercial estimate и company profile не копируются в competition registry;
- persisted payload повышен до версии 9, analyzer — до версии 10; provider schema/format v4,
  prompt v6, context v5, citation resolver v1, legal policy v1 и financial policy v1 не изменены;
- current v9 cache локально пересчитывается и exact-сверяется, legacy v1–v8 остаётся unavailable,
  future/corrupt/tampered data и duplicate JSON keys обрабатываются fail-closed без новой таблицы
  или миграции SQLite;
- existing AI tab и JSON/HTML exporter показывают четыре статуса, priorities, actions, current
  citations, warnings и competition disclaimer с HTML escaping;
- RM-107 score/recommendation/actions/evidence/confidence и абсолютный приоритет critical
  stop-factor не изменены; competition assessment не входит в decision evidence;
- локальная приёмка на Python 3.12.7: target `468 passed in 14.72s`, full
  `1389 passed in 67.60s`, Ruff (`517 files`), mypy (19 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #47 слит в `main` 15 июля 2026 года коммитом `4ebbf6c`. Post-merge Quality Gate run
`29422296807` успешно прошёл на Python 3.12 (`1389 passed in 100.83s`) и Python 3.13
(`1389 passed in 70.65s`); Ruff (`517 files`), mypy (19 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих версиях.
Новая AI-stage, provider call, repository, БД или миграция не добавлены. RM-122 соответствует
Definition of Done; следующим активным этапом назначен RM-123, application-код которого требует
отдельного аудита полноты документации.

## RM-123 — implementation acceptance

Статус реализации: `DONE` (PR #49, merge `759f015`).

- canonical local inventory объединяет existing `TenderDocumentStore.list_documents()` и latest
  `TenderDocumentTextService.list_results()` по `document_key`, сохраняет download failures и
  archive members, использует stable ordering и safe identity без private paths, URLs или errors;
- pure documentation-completeness policy формирует deterministic status, counts, issues, stable
  IDs и actions без второго provider call, text/keyword matching, network, filesystem, DB или
  money calculations; provider `missing_documents` не управляет локальной оценкой;
- inventory и decision-relevant statuses/checksum/kind/inclusion/truncation входят в context
  fingerprint; перестановка input records fingerprint не меняет;
- persisted payload повышен до версии 10, analyzer — до версии 11, context — до версии 6;
  provider schema/format v4, prompt v6, citation resolver v1 и legal/financial/competition policy
  v1 не изменены;
- current v10 inventory строго валидируется, assessment локально пересчитывается и exact-сверяется;
  legacy v1–v9 остаётся unavailable, future/corrupt/tampered data и duplicate JSON keys
  обрабатываются fail-closed без новой таблицы или миграции SQLite;
- existing AI tab и JSON/HTML exporter показывают четыре русских статуса, disclaimer, counts,
  coverage, safe inventory, issues/actions и warnings; AI `missing_documents` маркируется отдельно,
  external values экранируются;
- сохранены один classifier, analyzer/service/Orchestrator/repository/provider call, одна
  `RUNNING_AI` stage и существующие UI/export workflow;
- RM-107 score/recommendation/actions/evidence/confidence и абсолютный приоритет critical
  stop-factor не изменены; documentation assessment не входит в decision evidence;
- локальная приёмка на Python 3.12.7: target `589 passed in 15.07s`, full
  `1442 passed in 52.38s`, Ruff (`519 files`), mypy (20 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #49 слит в `main` 15 июля 2026 года коммитом `759f015`. Post-merge Quality Gate run
`29430495132` успешно прошёл на Python 3.12 (`1442 passed in 85.09s`) и Python 3.13
(`1442 passed in 81.29s`); Ruff (`519 files`), mypy (20 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих версиях.
Первый Python 3.12 attempt завершился transient native Windows heap crash `0xc0000374`; rerun
того же merge SHA прошёл полностью без изменения кода. Новая AI-stage, provider call,
repository, БД или миграция не добавлены. RM-123 соответствует Definition of Done; следующим
активным этапом назначен RM-124, application-код которого требует отдельного аудита повторной
проверки AI.

## RM-124 — implementation acceptance

Статус реализации: `DONE` (PR #51, merge `cfd044e`).

- pure recheck policy v1 exact-сравнивает два current-compatible анализа одного registry key,
  fingerprint, provider/model и version contract; technical IDs/timestamps/response IDs и
  repository warnings не создают semantic delta;
- учитываются summary, final conclusion, missing documents, scoped statuses и только locally
  verified findings; exact key `scope + category + citation_id` формирует deterministic
  added/removed/modified deltas без heuristic matching или повышения unverified findings;
- отдельные service/orchestrator `recheck` один раз строят context/fingerprint, захватывают exact
  append-only baseline до current save и вызывают existing analyzer ровно один раз без cache
  return, automatic retry, critic pipeline или stale fallback;
- repository read failure не блокирует current request; provider failure даёт
  `current_unavailable`, не заменяется baseline и не сохраняется;
- existing AI dialog использует confirmation, background worker, per-registry guard и result
  block в той же вкладке; новая `RUNNING_AI` stage или UI tab не добавлены;
- existing JSON/HTML exporter принимает optional safe `ai_recheck`; обычный export path не
  изменён, full analysis payload/raw response/URL/path/credentials/document text не экспортируются;
- provider schema/format v4, prompt v6, payload v10, analyzer v11, context v6, citation resolver
  v1 и physical SQLite schema не изменены; recheck policy имеет версию 1;
- сохранены один provider call site, analyzer/service/Orchestrator/repository, одна
  `RUNNING_AI` stage и existing runtime graph;
- RM-107 score/recommendation/actions/evidence/confidence/commercial estimate и абсолютный
  приоритет critical stop-factor не изменены;
- локальная приёмка на Python 3.12.7: target `150 passed in 5.70s`, full
  `1466 passed in 56.89s`, Ruff (`521 files`), mypy (20 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #51 слит в `main` 15 июля 2026 года коммитом `cfd044e`. Post-merge Quality Gate run
`29437124384` успешно прошёл на Python 3.12 (`1466 passed in 108.68s`) и Python 3.13
(`1466 passed in 80.07s`); Ruff (`521 files`), mypy (20 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих версиях.
RM-124 соответствует Definition of Done; следующим активным этапом назначен RM-125, application-
код которого требует отдельного аудита стабилизации AI-платформы.

## RM-125 — implementation acceptance

Статус реализации: `DONE` (PR #53, merge `bdceb70`).

- введён единый immutable execution contract v1, который exact-связывает provider, model,
  provider schema/format, prompt, payload, analyzer, context, citation и recheck versions;
- typed repository lookup пропускает corrupt/future/mismatched rows, находит более старую exact
  current-compatible запись и не использует mutable production warning state;
- empty-source analysis создаёт valid provenance без provider call, сохраняется один раз и
  переиспользуется только при exact contract/fingerprint match;
- allowlisted provider failures отображаются fixed bounded warnings без raw exception text,
  credentials или private paths; automatic retry и stale fallback не добавлены;
- pure cacheability predicate запрещает persistence без exact key/fingerprint/current contract,
  payload и provenance;
- per-key coordinator сериализует одинаковые run/recheck, допускает параллельность разных ключей
  и очищает lock state после исключения;
- participation decision больше не читает implicit latest AI analysis и получает текущий
  результат только явно через существующий full-analysis path;
- analyzer повышен до v12; provider schema/format v4, prompt v6, payload v10, context v6,
  citation resolver v1 и recheck policy v1 не изменены;
- сохранены один provider call site, analyzer/service/Orchestrator/repository, одна
  `RUNNING_AI` stage и existing runtime graph; новая AI-stage, provider call, repository,
  БД, таблица или migration не добавлены;
- RM-107 score/recommendation/actions/evidence/confidence/commercial estimate и абсолютный
  приоритет critical stop-factor не изменены;
- локальная приёмка на Python 3.12.7: target `315 passed in 7.15s`, full
  `1496 passed in 58.68s`, Ruff (`523 files`), mypy (20 файлов), secret scan, dependency audit
  и diff-check успешны.

PR #53 слит в `main` 15 июля 2026 года коммитом `bdceb70`. Post-merge Quality Gate run
`29450245855` успешно прошёл на Python 3.12 (`1496 passed in 95.46s`) и Python 3.13
(`1496 passed in 61.69s`); Ruff (`523 files`), mypy (20 файлов), secret scan, offline,
migration, composition и build smoke tests, а также dependency audit успешны на обеих версиях.
RM-125 соответствует Definition of Done; следующим активным этапом назначен RM-126, application-
код которого требует отдельного аудита раздела Тендеры.

# RM-126–RM-140 — универсальный поиск и площадки
- [x] RM-126 — Аудит раздела Тендеры.
- [x] RM-126.1 — Аудит и укрепление текущего провайдера ЕИС.

## RM-126.1 — implementation acceptance

- baseline `45d63ff4eba95d8443baed37275e619280634e12`, отдельная ветка
  `feat/rm-126-eis-parser-hardening-stage-1`; audit-only commit `955ec6a` создан до production changes;
- обязательный аудит и решения размещены в `docs/EIS_PARSER_STAGE_1_AUDIT.md` и
  `docs/EIS_PARSER_ARCHITECTURE.md`; operations runbook — в `docs/EIS_PARSER_OPERATIONS.md`;
- существующая цепочка `AsyncHttpClient → AsyncProviderSearchEngine → AsyncEisTenderProvider →
  UnifiedTender → CollectorStateRepository` сохранена без дублирующих сервисов и без DB migration;
- реализованы search diagnostics `eis-search-v3`, law router, detail adapters
  `eis-notice-44-v1`/`eis-notice-223-v1`, documents version `eis-documents-v2`, обязательная
  fail-closed validation, allowed hosts и typed structural/access/URL errors;
- `get_tender()` делает exact search discovery, один detail request, выбирает adapter по закону и
  возвращает объединённый `UnifiedTender`; документы при этом не скачиваются;
- network/parser health разделены; snapshots выключены по умолчанию и сохраняют только body,
  allowlisted metadata и sanitized error; live canary ограничен одним search request и 10 items;
- fixtures покрывают 44-ФЗ, 223-ФЗ, mixed, empty, structural drift, missing optional fields,
  documents, CAPTCHA, access denied, maintenance и malformed HTML;
- локальная приёмка: EIS/Collector target `69 passed in 6.35s`, full `1524 passed in 54.24s`,
  repository secret scan, Ruff check/format (`537 files`), mypy (20 файлов),
  offline/migration/import/composition/build smoke и dependency audit успешны;
- live canary не запускался автоматически против внешней ЕИС; выполнен только offline `--help` smoke;
- feature PR #58 слит в `main` коммитом `b6369c8`
  (`b6369c85791b9c06a97f03a1fbb2504c88a1dea7`);
- post-merge Windows Quality Gate run `29460395144` успешен: Python 3.12 —
  `1524 passed in 95.38s`, Python 3.13 — `1524 passed in 71.31s`; Ruff check/format
  (`537 files`), mypy (20 файлов), secret scan, offline/migration/import/composition/build smoke и
  dependency audit успешны на обеих версиях;
- RM-126.1 и общий RM-126 соответствуют Definition of Done; RM-127 назначен единственным
  `IN PROGRESS`, RM-128–RM-200 остаются `PLANNED`.

## RM-126 — acceptance аудита

- entry gate: RM-125 feature PR #53 (`bdceb70`) и closeout PR #54 (`7d51159`) merged;
  post-merge Windows Quality Gate run `29450245855` успешен на Python 3.12/3.13;
- audit baseline: `7d51159a0fcbe21a457276ab29cc80fd1a2eb985`, Windows 10 `10.0.19045.0`,
  Python 3.12.7, `Russian Standard Time`/UTC+03:00;
- docs-only evidence и handoff зафиксированы в `docs/RM-126_AUDIT.md`,
  `docs/RM-126_REQUIREMENTS.md` и плане `2026-07-16-rm-126-tenders-audit.md`;
- приняты D-01–D-10: modern tender page, async Collector target + sync facade, единый provider
  catalog/manager/keyring/profile/normalization/persistence contract, strict Decimal/aware-time policy и
  распределение C1–C20 по RM-127–RM-140;
- baseline validation: target `395 passed in 36.20s`, full `1496 passed in 63.74s`; final audit branch:
  target `395 passed in 33.99s`, full `1496 passed in 61.27s`; Ruff check и
  format (`523 files`), mypy (20 файлов), secret scan, offline/migration/import/composition/build smoke,
  dependency audit и diff-check успешны;
- подтверждённые HIGH handoff: embedded legacy UI, два search orchestration path, отсутствие общего
  tender shutdown и неоднородная timezone policy; publication blocker не обнаружен;
- audit PR #55 слит в `main` коммитом `f09d07e`
  (`f09d07ebb1a15acb42279d3b8f7e0393c8d84afc`); production-код, зависимости, DB schema и
  migrations не изменены;
- post-merge Windows Quality Gate run `29453928900` успешен: Python 3.12 —
  `1496 passed in 76.46s`, Python 3.13 — `1496 passed in 157.55s`; Ruff check/format
  (`523 files`), mypy (20 файлов), secret scan, offline/migration/import/composition/build smoke и
  dependency audit успешны на обеих версиях;
- Общий аудит RM-126 был принят, затем владелец проекта явно переоткрыл этап для позднего подэтапа
  RM-126.1. Условия повторного закрытия выполнены: feature PR #58 merged, post-merge run
  `29460395144` успешен и результаты зафиксированы этим docs-only closeout.

## RM-127 — final acceptance

- baseline `f34c66b7adf2c175c5a48f17db4f12e154745813`; audit/plan зафиксированы docs-only
  commit `13dfb83` до application changes; characterization contract — commit `cb21b82`;
- существующий workspace выделен как одна `TenderWorkspacePage(QWidget)`; legacy `MainWindow`
  сохранён тонким standalone wrapper с одной page;
- production `ModernMainWindow` больше не создаёт hidden legacy shell и не использует
  `_legacy_window`/`takeCentralWidget()`/direct legacy fields;
- сохранены exact 8 top-level и 6 nested settings tabs, добавлены stable keys/objectNames;
- Dashboard/topbar используют narrow page API; topbar сохраняет price/equipment compatibility
  semantics и не запускает network, search engine или Collector;
- один существующий `TenderSearchUiController` переиспользует те же 7 direct и 2 scheduler actions;
  menu/toolbar/shortcuts/dialogs и C11 full-analysis route сохранены;
- RM-127 focused: `9 passed in 6.11s`; соседний UI/controller contour: `68 passed in 9.58s`;
  C11 parity: `51 passed in 4.09s`; повторный combined focused: `54 passed in 31.02s`;
- полный локальный pytest дважды успешен: `1532 passed in 55.91s` и
  `1532 passed in 77.25s`;
- repository secret scan, Ruff check, Ruff format (`540 files`), mypy (20 файлов),
  offline (`2 passed`), migration (`5 passed`), import, composition (`1 passed`), build (`6 passed`),
  dependency audit и diff-check успешны;
- DB/schema/migrations/dependencies/providers/search/Collector/decision/AI не изменены;
- feature PR #60 слит в `main` коммитом `0b95567`
  (`0b9556799a20ddbf7338476fe76f602e7ff79d07`);
- PR Quality Gate run `29488228903` успешен: Python 3.12 — `1532 passed in 94.14s`,
  Python 3.13 — `1532 passed in 83.76s`; остальные обязательные gates успешны;
- post-merge Quality Gate run `29489511239` успешен на точном merge SHA: Python 3.12 —
  `1532 passed in 82.83s`, Python 3.13 — `1532 passed in 145.43s`; Ruff check/format
  (`540 files`), mypy (20 файлов), secret scan, offline/migration/import/composition/build smoke и
  dependency audit успешны на обеих версиях;
- RM-127 соответствует Definition of Done и переведён в `DONE`; его closeout ранее активировал
  RM-128. Актуальный переход RM-128 → RM-129 зафиксирован в следующем разделе.

- [x] RM-127 — Новая структура вкладок.
- [x] RM-128 — Единая панель поиска.

  Feature implementation завершена и слита feature PR #62 в `main` коммитом `a67f5df`
  (`a67f5df331f8257799e24a9ef3980c6feea69c7a`). Audit-first gate зафиксирован commit `39605d0` до
  application changes. Реализованы один
  `TenderUnifiedSearchPanel`, pure immutable request boundary и один existing-controller seam создания
  `_CollectorRunWorker`; topbar использует page → panel → existing async Collector path, а legacy
  profile dialog сохраняет sync rollback path. Новые repository/engine/Collector/provider catalog,
  DB/migration/profile schema/dependency не добавлены.

  Локальная acceptance Windows/Python 3.12.7:

  - focused RM-128/controller/bootstrap: `23 passed in 5.20s`;
  - соседний RM-127/UI/Collector contour: `66 passed in 8.80s`;
  - full pytest: `1552 passed in 61.49s (0:01:01)`;
  - secret scan: `Repository secret scan passed.`;
  - Ruff check: `All checks passed!`; format: `545 files already formatted`;
  - mypy: `Success: no issues found in 20 source files`;
  - offline smoke: `2 passed in 5.38s`; migration smoke: `5 passed in 3.12s`;
  - import smoke: `DashboardController`; composition smoke: `1 passed in 0.27s`;
  - build/release smoke: `6 passed in 4.17s`;
  - dependency audit: `No known vulnerabilities found`; editable project skipped;
  - `git diff --check` и clean worktree: success до evidence update.

  PR Quality Gate run `29499175129` успешен: Python 3.12 — `1552 passed in 67.74s`, Python 3.13 —
  `1552 passed in 97.95s`. Exact-SHA post-merge run `29499519358` успешен: Python 3.12 —
  `1552 passed in 169.06s`, Python 3.13 — `1552 passed in 73.62s`; initial Python 3.12 native access
  violation не воспроизвёлся при rerun того же SHA. На обеих версиях успешны secret scan, Ruff
  check/format (`545 files`), mypy (20 файлов), offline/migration/import/composition/build smoke и
  dependency audit. RM-128 соответствует Definition of Done и закрывается docs-only closeout;
  RM-129 становится единственным `IN PROGRESS`.

- [x] RM-129 — Универсальные бизнес-профили.

  Feature implementation подготовлена на ветке `feat/rm-129-universal-business-profiles`, HEAD
  `a99252b32d818dd6d07f9aaa818cbeb1cdbafb00`. Audit/plan зафиксированы docs-only commit `ddb8427`
  до application changes; expected-red contract — commit `3331131`.

  Существующий `CompanyCapabilityProfileRepository` пишет schema v2 в тот же
  `company_capability_profile.json`, безопасно читает v1 in memory и различает
  missing/current/migrated/corrupt/future. Content-bound confirmation, explicit currency и pure frozen
  `BusinessCapabilityProjection` направляют manual/Collector score и stop-factor через один confirmed
  facts snapshot. Сохранены public compatibility APIs, score components/maxima/explanations/bands,
  matching catalog и абсолютный приоритет critical stop; неподтверждённые capabilities fail closed.
  Existing runtime/controller/dialog разделяют один repository owner; DB, migrations, network,
  dependencies, AI, saved search profiles и RM-130+ scope не изменены.

  Локальная acceptance на Windows/Python 3.12.7: focused `76 passed in 5.25s`, neighbor
  `38 passed in 6.85s`, adjacent summary/full-analysis `51 passed in 3.53s`, full pytest
  `1623 passed in 70.35s`. Secret scan, Ruff check/format (`549 files`), mypy (20 файлов),
  offline/migration/import/composition/build smoke, dependency audit и diff-check успешны.

  Feature PR #64 слит в `main` коммитом `f9b43c3`
  (`f9b43c37bb5c7e631e4851cde2b39c1178d34239`). PR Quality Gate run `29522220375` успешен:
  Python 3.12 — `1623 passed in 164.70s`, Python 3.13 — `1623 passed in 63.03s`. Exact-SHA
  post-merge run `29522737754` успешен: Python 3.12 — `1623 passed in 69.59s`, Python 3.13 —
  `1623 passed in 103.67s`; на обеих версиях прошли secret scan, Ruff check/format (`549 files`),
  mypy (20 файлов), offline/migration/import/composition/build smoke и dependency audit. RM-129
  соответствует Definition of Done и закрывается этим docs-only closeout; RM-130 становится
  единственным `IN PROGRESS`.
- [x] RM-130 — Сохранённые поисковые профили.
  - Audit/plan и expected-red contract зафиксированы отдельными коммитами `09d60cc` и `f206c0d`;
    feature implementation — коммитами `a60733e`, `130f637`, `b8b49f7` в одном existing owner/path.
  - Локальная feature acceptance на Windows/Python 3.12.7 успешна: focused `82 passed in 9.68s`,
    neighbor `64 passed in 6.33s`, full pytest `1656 passed in 60.73s`; secret scan, Ruff
    check/format (`554 files`), mypy (20 файлов), offline/migration/import/composition/build smoke,
    dependency audit и diff-check успешны.
  - Feature PR #66 слит в `main` коммитом `3a4d530`
    (`3a4d53067b7b0f8eaf0b5969c139284c9d5ed987`). PR Quality Gate run `29533900495` успешен:
    Python 3.12 — `1656 passed in 101.17s`, Python 3.13 — `1656 passed in 150.85s`.
  - Exact merge-SHA run `29534568925` успешен: Python 3.12 — `1656 passed in 141.74s`, Python 3.13 —
    `1656 passed in 66.21s`; на обеих версиях прошли secret scan, Ruff check/format (`554 files`),
    mypy (20 файлов), offline/migration/import/composition/build smoke и dependency audit.
  - RM-130 соответствует Definition of Done и закрывается этим docs-only closeout; RM-131 становится
    единственным `IN PROGRESS`.
- [x] RM-131 — Настройки площадок.
  - Entry gate пройден на exact baseline `e76a602`: RM-130 закрыт feature merge, успешным exact-SHA
    Windows gate и отдельным docs-only closeout; RM-131 — единственный активный этап.
  - Audit/plan подтверждают два текущих settings JSON owner и divergence: manager читает persisted
    commercial settings, а стандартный Collector run строит commercial catalog без этого path.
  - Выбран existing `collector_provider_settings.json` schema v2 как единственная canonical non-secret
    persistence boundary за `CollectorProviderManager`; health, C19 verification, keyring и legacy
    manual connections остаются отдельными владельцами своих данных.
  - Split-v1 migration сохраняет оба источника byte-for-byte backup, не удаляет legacy commercial
    файл и не переписывает profile/schedule references; aliases разрешаются read-compatibly.
  - Baseline Windows/Python 3.12.7: provider/UI/factory/session/scheduler/legacy contour
    `41 passed in 9.41s`, full pytest `1656 passed in 80.26s`; post-closeout GitHub run
    `29535586550` успешен на Python 3.12/3.13.
  - Audit-first sequence соблюдена: docs-only `243ab56`, expected-red `4c13913` (`7 errors in
    4.49s` только по отсутствующим RM-131 boundaries), implementation/regression `83f4c0a`,
    `b7398bd`, `a27b44b`.
  - Feature acceptance Windows/Python 3.12.7: focused `30 passed in 4.37s`, neighbor
    `76 passed in 12.01s`, full pytest `1686 passed in 63.19s`; secret scan, Ruff check/format
    (`562 files`), mypy, offline/migration/import/composition/build smoke, dependency audit и
    diff/status checks успешны.
  - Feature PR #68 слит в `main` коммитом `bbfd8e3`
    (`bbfd8e3b858a29f07d7b55fde5fdb5a80a1d9cf2`). PR Quality Gate run `29538903447` успешен:
    Python 3.12 — `1686 passed in 90.24s`, Python 3.13 — `1686 passed in 101.46s`.
  - Exact merge-SHA run `29562019173` успешен: Python 3.12 — `1686 passed in 105.77s`, Python 3.13 —
    `1686 passed in 69.02s`; на обеих версиях прошли secret scan, Ruff check/format (`562 files`),
    mypy (20 файлов), offline/migration/import/composition/build smoke и dependency audit.
  - RM-131 соответствует Definition of Done и закрывается этим docs-only closeout; RM-132 становится
    единственным `IN PROGRESS`.
- [x] RM-132 — Безопасный ввод API и credentials.
  - Entry gate пройден на exact baseline `d86b886`: RM-131 закрыт feature merge, успешным exact-SHA
    Windows gate и отдельным docs-only closeout; final run `29562744319` успешен на Python 3.12/3.13.
  - Audit подтверждает один vault owner `app.security.secrets` и gaps в application/UI contract:
    MOS save делает readback, ordinary provider states читают keyring, commercial payload показывает
    masked fragment, legacy UI создаёт arbitrary `platform:<name>` identity, backend errors не bounded.
  - Выбран storage-free typed credential façade за `CollectorProviderManager` поверх existing keyring;
    runtime `load_secret` сохраняется только для adapters, startup остаётся no-keyring/no-network.
  - Baseline Windows/Python 3.12.7: focused credential/provider/UI/support contour
    `71 passed in 6.41s`, full pytest `1686 passed in 74.30s`.
  - Audit-first sequence зафиксирована коммитами `25b2eed` и `131f9a8`; implementation/regression —
    `5876bc9`, `f9365ea`, `3b26d15`. Storage-free façade переиспользует единственный
    `app.security.secrets`, ordinary manager state остаётся no-keyring/no-network, а UI не делает
    saved-value readback и не показывает masked fragments.
  - Локальная feature acceptance Windows/Python 3.12.7: focused `21 passed in 3.52s`, neighbor
    `110 passed in 8.59s`, full pytest `1707 passed in 64.89s`; secret scan, Ruff check/format
    (`570 files`), mypy (20 файлов), offline/migration/import/composition/build smoke, dependency
    audit и diff check успешны.
  - Feature PR #70 слит в `main` коммитом `1ae9c36`
    (`1ae9c36605043e35333dffc60a6077c16fbd19f4`).
  - PR Quality Gate run `29565942602` успешен: Python 3.12 —
    `1707 passed in 186.83s`, Python 3.13 — `1707 passed in 76.36s`.
  - Exact merge-SHA run `29567132554` успешен: Python 3.12 —
    `1707 passed in 121.29s`, Python 3.13 — `1707 passed in 79.55s`; все workflow jobs
    завершились `success`.
  - RM-132 соответствует Definition of Done и закрывается этим docs-only
    closeout; RM-133 становится единственным `IN PROGRESS`.
- [x] RM-133 — Ручное добавление площадки (`DONE`).
  - Активирован только после feature merge RM-132 и успешного exact merge-SHA
    Windows gate; RM-134+ остаются `PLANNED`.
  - Audit baseline `76687a50fe679bfcecc8cc48796fa4fcfae2bba6`; обязательные границы и
    implementation plan зафиксированы в `docs/RM-133_MANUAL_PROVIDER_AUDIT.md` и
    `docs/RM-133_IMPLEMENTATION_PLAN.md` до application-кода.
  - Baseline Windows/Python 3.12.7: focused provider/settings/UI contour
    `75 passed, 2 warnings in 12.48s`, full pytest
    `1707 passed, 2 warnings in 78.64s`.
  - Выбрано расширение единственного `ProviderEnablementRepository` и
    `collector_provider_settings.json` до schema v3; второй catalog/store, SQLite migration,
    legacy auto-import, credentials, protocol/adapter/connection test и live network запрещены.
  - Docs-only audit `31e1456`, expected-red contract `d3f8906` и feature implementation
    `3931d7b` соблюдают mandatory ordering; acceptance зафиксирована в
    `docs/RM-133_MANUAL_PROVIDER_ACCEPTANCE.md`.
  - Local feature acceptance Windows/Python 3.12.7: focused `51 passed in 4.31s`, neighbor
    `160 passed, 2 warnings in 12.65s`, full pytest
    `1758 passed, 2 warnings in 66.57s`; secret scan, Ruff check/format (`578 files`), mypy,
    offline/database/import/composition/build smoke, dependency audit и diff check успешны.
  - Feature PR #72 слит в `main` коммитом `c067b5e`
    (`c067b5ecbc24428906dd006abe1e0ee6eef48e12`).
  - PR Quality Gate run `29572356676` успешен: Python 3.12 —
    `1758 passed, 2 warnings in 93.74s`, Python 3.13 —
    `1758 passed, 2 warnings in 63.88s`.
  - Exact merge-SHA run `29573356516` успешен: Python 3.12 —
    `1758 passed, 2 warnings in 104.98s`, Python 3.13 —
    `1758 passed, 2 warnings in 174.96s`; все workflow jobs завершились `success`.
  - RM-133 соответствует Definition of Done и закрывается этим docs-only closeout;
    RM-134 становится единственным `IN PROGRESS`.
- [x] RM-134 — Выбор протокола (`DONE`).
  - Активирован только после feature merge RM-133 и успешного exact merge-SHA Windows gate;
    RM-135+ оставались `PLANNED`.
  - Audit baseline `38be7babdd0532ef88a1fbeff0acaed75737ea24`; audit и implementation plan
    зафиксированы в `docs/RM-134_PROTOCOL_SELECTION_AUDIT.md` и
    `docs/RM-134_IMPLEMENTATION_PLAN.md` до application-кода.
  - Baseline Windows/Python 3.12.7: focused provider/settings/UI/runtime contour
    `120 passed, 2 warnings in 16.47s`, full pytest
    `1758 passed, 2 warnings in 80.64s`.
  - Closed API/RSS/FTP/FTPS policy/selection contract добавлен без executable adapter,
    parser, connection test, DNS/live network или credential values; manual provider после
    выбора остаётся registration-only/non-runnable и получает lifecycle `ADAPTER_REQUIRED`.
  - Единственный `ProviderEnablementRepository` и `collector_provider_settings.json`
    повышены до schema v4 с in-memory v3 migration, byte-exact backup, atomic replace и
    compare-and-replace по `updated_at`; второй store/catalog и DB migration не создавались.
  - Audit/plan `5889944`, expected-red tests `6610f11`, feature implementation `4f1d83c`,
    acceptance `bf9fd12` и formatting guard `9162beb` соблюдают mandatory ordering;
    acceptance находится в `docs/RM-134_PROTOCOL_SELECTION_ACCEPTANCE.md`.
  - Local feature acceptance Windows/Python 3.12.7: focused `38 passed in 4.12s`, neighbor
    `169 passed, 2 warnings in 14.66s`, full pytest
    `1796 passed, 2 warnings in 69.63s`; secret scan, Ruff check/format (`583 files`), mypy,
    offline/database/import/composition/build smoke, dependency audit и diff check успешны.
  - Feature PR #74 слит в `main` коммитом `7ef0378`
    (`7ef0378315f9ef76046a651d1211f3da191b7719`).
  - PR Quality Gate run `29577913214` успешен: Python 3.12 —
    `1796 passed, 2 warnings in 113.76s`, Python 3.13 —
    `1796 passed, 2 warnings in 87.91s`.
  - Exact merge-SHA run `29578571237` успешен: Python 3.12 rerun —
    `1796 passed, 2 warnings in 84.87s`, Python 3.13 —
    `1796 passed, 2 warnings in 124.47s`; все обязательные jobs завершились `success`.
    Первый Python 3.12 attempt завершился native Windows heap abort `0xc0000374` без test
    assertion; failed-only rerun на том же SHA прошёл без code/doc изменений.
  - RM-134 соответствует Definition of Done и закрывается этим docs-only closeout;
    RM-135 становится единственным `IN PROGRESS`.
- [x] RM-135 — Безопасный конструктор адаптера (`DONE`).
  - Audit baseline `9008f8caee02c09221ab9e2e7da1c130420d0689`; audit/plan commit
    `b0f1048` создан до application-кода, expected-red contract — `e7b9121`.
  - Один pure `manual_adapter.py` добавил immutable/versioned `ManualAdapterSpec` v1,
    deterministic semantic fingerprint, restricted canonical mapping/transforms, hard resource
    limits и bounded offline JSON/XML/RSS/Atom/CSV preview.
  - Existing `ProviderEnablementRepository` повышен до schema v5 с in-memory v4 migration,
    byte-exact backup, atomic replace, stale-write guard, semantic no-op, monotonic revision и
    bounded rollback history; SQLite/keyring/sample persistence не изменялись.
  - Existing async factory делегирует explicit API/RSS/FTP/FTPS build static compiler; ordinary
    composition не перечисляет manual adapters. Compiled instance соответствует
    `AsyncTenderProvider`, но live methods fail closed `connection_test_required`.
  - Existing manager/catalog/dialog/controller получили typed compile/preview/save/clear/rollback
    commands и offline wizard; manual source остаётся disabled/unverified/non-runnable без active
    health/connection control.
  - Local acceptance: focused `27 passed in 3.65s`, neighbor
    `205 passed, 2 warnings in 13.66s`, full pytest
    `1823 passed, 2 warnings in 64.15s`; secret scan, Ruff check/format (`592 files`), required и
    changed-contour mypy, workflow smokes, dependency audit и diff-check успешны.
  - Feature PR #76 слит в `main` коммитом `306b209`
    (`306b20977b6c23956488dc471da63af17f197e25`). PR Quality Gate run `29584304208`
    успешен: Python 3.12 — `1823 passed, 2 warnings in 59.52s`, Python 3.13 —
    `1823 passed, 2 warnings in 88.40s`.
  - Exact merge-SHA run `29586643112` успешен: Python 3.12 rerun —
    `1823 passed, 2 warnings in 86.41s`, Python 3.13 —
    `1823 passed, 2 warnings in 88.19s`; первый Python 3.12 attempt завершился native Windows
    access violation без test assertion, failed-only rerun того же SHA прошёл без изменений.
  - RM-135 соответствует Definition of Done и закрывается этим docs-only closeout;
    RM-136 становится единственным `IN PROGRESS`.
- [x] RM-136 — Тест подключения (`DONE`).
  - Entry gate пройден на exact baseline `ef5cee6d0ef2079f23866fbb809575806f52a0d4`;
    audit/plan commit `f4bb93a` создан до application-кода, expected-red contract — `31da549`.
  - Existing health evidence повышен до schema v2, provider settings — до schema v6 с typed
    FTPS implicit/explicit mode; in-memory migration, byte-exact backup, atomic replace,
    stale compare-and-replace и revision-aware binding сохранены.
  - Explicit health check использует bounded HTTP/RSS/FTP/FTPS transport, all-answer DNS
    classification, global-address allow policy, pinned TLS, redirect/unsafe-target rejection,
    runtime-only credential resolution и existing RM-135 parser/mapping preview.
  - Current `PASSED/HEALTHY` evidence с code-owned TTL 15 минут требуется для explicit enablement
    и каждого admission; success не включает provider автоматически, binding mutations
    инвалидируют evidence, plaintext FTP остаётся degraded.
  - Local acceptance на feature HEAD `fcf68ae3df340a62b4ce07e2e088a8a63a8dad5b`:
    focused `36 passed in 4.34s`, full pytest `1859 passed, 2 warnings in 70.14s`; secret scan,
    Ruff check/format (`606 files`), required и owner-contour mypy, workflow smokes,
    dependency audit и diff-check успешны.
  - Feature PR #78 слит в `main` merge commit
    `d84288ab74553e500ad9eaf9f51a091404490551`. PR Quality Gate run `29606049619` успешен:
    Python 3.12 — `1859 passed, 2 warnings in 142.75s`, Python 3.13 —
    `1859 passed, 2 warnings in 87.96s`.
  - Exact merge-SHA run `29606492310` успешен: Python 3.12 —
    `1859 passed, 2 warnings in 93.95s`, Python 3.13 —
    `1859 passed, 2 warnings in 103.55s`; все обязательные jobs завершились `success`.
  - RM-136 соответствует Definition of Done и закрывается этим docs-only closeout;
    RM-137 становится единственным `IN PROGRESS`.
- [x] RM-137 — Отраслево-независимая нормализация (`DONE`).
  - Audit/plan commit `32a1257` создан до application-кода; expected-red contract — `209acd7`.
  - Existing `UnifiedTender`, `TenderNormalizer`, Collector/repository/dedup/verification и DI paths
    переиспользованы. Pure normalization contract v1 добавил conservative text/ID, strict Decimal,
    aware UTC datetime, safe URL/collection normalization, bounded diagnostics/provenance и
    versioned semantic fingerprint без нового model/repository/DB/search engine.
  - Collector, legacy provider-result path и offline manual mappings используют один normalizer;
    live manual/commercial admission остаётся fail-closed. Collector schema 14, Registry schema 1,
    legacy payload readers и RM-107 deterministic decision/scoring/critical stop-factor сохранены.
  - Локальная acceptance: focused `20 passed in 5.13s`, full pytest
    `1879 passed, 2 warnings in 98.80s`; secret scan, Ruff/format (`608 files`), required и
    owner-contour mypy, workflow smokes, dependency audit и diff-check успешны.
  - Feature PR #81 слит в `main` merge commit
    `e38c8c13f0ec822fde76bdbc6319a18a05fd500b`. PR Quality Gate run `29614656151` успешен:
    Python 3.12 — `1879 passed, 2 warnings in 147.32s`, Python 3.13 —
    `1879 passed, 2 warnings in 96.35s`.
  - Exact merge-SHA run `29615080804` успешен: Python 3.12 —
    `1879 passed, 2 warnings in 105.98s`, Python 3.13 —
    `1879 passed, 2 warnings in 94.45s`; все обязательные jobs завершились `success`.
  - RM-137 соответствует Definition of Done и закрывается этим docs-only closeout;
    RM-138 становится единственным `IN PROGRESS`.
- [x] RM-138 — Параллельный поиск (`DONE`).
  - Audit/contract/plan commit `bd3880d` создан до application-кода; expected-red lifecycle
    contract — `7360125`.
  - Existing production async Collector coordinator, run-session admission, provider factory,
    RM-137 normalizer/deduplicator, repository/DB, HTTP retry и DI paths переиспользованы;
    третий engine, второй retry/model/repository/DB не создавались.
  - Immutable revisioned run/provider snapshots обеспечивают exact states, aware UTC,
    monotonic provider/overall deadlines, bounded concurrency и engine-owned progress.
  - Cooperative idempotent cancellation сохраняет accepted partial results, отвергает late
    completions и публикует один terminal snapshot; legacy blocking-thread limit документирован.
  - Canonical partial output нормализуется/deduplicate один раз и не зависит от completion order;
    slow progress subscriber изолирован bounded dispatcher.
  - Safe typed errors проходят через outcomes, persistence и UI без raw exception/URL/credential;
    UI не блокирует event loop и не содержит progress/business merge logic.
  - Sync `TenderSearchEngine`, RM-107 score/recommendation/hard-exclusion и critical stop-factor
    priority сохранены.
  - Локальная acceptance: full pytest `1892 passed, 2 warnings in 125.33s`; secret scan,
    Ruff/format (`611 files`), mypy, workflow smokes, five repeated race runs и dependency audit
    успешны.
  - Feature PR #84 слит в `main` merge commit
    `593ed39c7b81efc8a67e36eef47ceadbbbaf46ca`. PR run `29619784410` успешен:
    Python 3.12 — `1892 passed, 2 warnings in 94.36s`, Python 3.13 —
    `1892 passed, 2 warnings in 111.15s`.
  - Exact merge-SHA run `29619998396` успешен: Python 3.12 —
    `1892 passed, 2 warnings in 102.67s`, Python 3.13 —
    `1892 passed, 2 warnings in 82.24s`; все обязательные jobs завершились `success`.
  - RM-138 соответствует Definition of Done и закрывается этим docs-only closeout;
    RM-139 становится единственным `IN PROGRESS`.
- [x] RM-139 — Мониторинг источников (`DONE`).
  - Audit/contract/plan commit `6ad5741` создан до application-кода; expected-red contract —
    `d9b2b97`.
  - Existing provider descriptors/configuration, connection evidence, Collector run/outcome and
    checkpoint persistence, C19 verification, schedule, health monitor/circuit, notifications и
    provider manager dialog переиспользованы без второго monitoring stack или schema bump.
  - Code-owned immutable snapshot раздельно показывает enablement, connection readiness,
    operational run/circuit, checkpoint freshness, C19 verification и schedule; aware UTC,
    explicit TTL/future-skew policy, safe typed UI/notifications и stable dedup сохранены.
  - Startup network I/O не добавлен; explicit health check остаётся отдельным действием и
    отклоняется при active Collector session. RM-107 deterministic decision/scoring/critical
    stop-factor priority не изменены.
  - Локальная feature acceptance: full pytest `1908 passed, 2 warnings in 120.62s`; secret scan,
    Ruff/format (`620 files`), required и owner-contour mypy, workflow smokes, five-cycle
    circuit/notification gate, dependency audit и diff-check успешны.
  - Feature PR #86 слит merge commit `41b547f67020b9645d915694c943b962b46ddc08`.
    PR Quality Gate run `29623757948` успешен: Python 3.12 —
    `1908 passed, 2 warnings in 82.11s`, Python 3.13 —
    `1908 passed, 2 warnings in 109.04s`.
  - Exact merge-SHA push run `29624355650` успешен: Python 3.12 —
    `1908 passed, 2 warnings in 120.67s`, Python 3.13 —
    `1908 passed, 2 warnings in 133.34s`; dependency audit и все обязательные jobs — `success`.
  - RM-139 соответствует Definition of Done и закрывается этим docs-only closeout; RM-140
    становится единственным `IN PROGRESS`.
- [x] RM-140 — Стабилизация поиска (`DONE`).
  - Audit/contract/plan commit `30b2f4a` создан до application-кода; characterization —
    `23d28ce`, expected-red contract — `ed150ae`.
  - Saved profiles, unified/manual search и scheduler используют один Collector admission;
    lifecycle, cancellation, shutdown, aware UTC, safe errors и sentinel exclusion закреплены
    contract-тестами.
  - Collector schema v14 и Registry schema v1 сохранены без migration/data copy; production
    legacy engine/service/runner retired, public compatibility API сохранён.
  - Локальная acceptance: full pytest `1946 passed, 2 warnings in 155.86s`; secret scan,
    Ruff/format (`630 files`), mypy, workflow smokes, five-cycle race gate и performance contour
    успешны.
  - Feature PR #88 слит merge commit `8c09ca6df469549b4ae50457b6924898a629c0d2`.
    PR Quality Gate run `29651765243` и exact merge-SHA run `29651986321` успешны на Python
    3.12/3.13; dependency audit и все обязательные jobs — `success`.
  - RM-107 deterministic decision/scoring/critical stop-factor priority не изменены. RM-140
    соответствует Definition of Done и закрывается этим docs-only closeout; RM-141 становится
    единственным `IN PROGRESS`.

# RM-141–RM-155 — полный редизайн и графики
- [x] RM-141 — Аудит UI (`DONE`).
  - Шесть обязательных audit-документов фиксируют production composition, 68 UI modules,
    16 user journeys, owner map, acceptance и точный handoff RM-142–RM-155.
  - 17 findings классифицированы как P0: 0, P1: 0, P2: 16, P3: 1; каждый actionable finding
    назначен ровно одному primary RM из RM-142–RM-155.
  - Read-only inventory и model benchmark не меняют production behavior; `app/`, dependencies,
    DB/schema/migrations, navigation/theme и deterministic decision boundaries не изменены.
  - Audit PR #90 слит merge commit `a2e8d0528a1b9c6378a543a5c9f2c5b762483c63`.
    PR Quality Gate run `29654916158` и exact merge-SHA run `29655095879` успешны на Python
    3.12/3.13; dependency audit и все обязательные jobs — `success`.
  - RM-141 соответствует Definition of Done и закрывается этим docs-only closeout; RM-142
    становится единственным `IN PROGRESS`.
- [x] RM-142 — Новая информационная архитектура (`DONE`).
  - Audit/contract/plan, characterization и expected-red зафиксированы до application changes.
  - Один immutable typed route registry и существующий `DashboardLayout` как единственный
    navigation owner устранили false peer placeholders без второго shell/router/page stack.
  - Primary navigation содержит Dashboard, Tenders и один Business Workflow; legacy aliases,
    tender deep links, quick actions, AI/settings/documents/scheduler/notifications сохранены.
  - Bounded in-memory history, allowlisted context, exact tender ID, workflow filters/stable
    selection и focus-origin return покрыты offline tests.
  - Локально: full pytest `1983 passed, 2 warnings`; secret scan, Ruff/format (`644 files`) и
    required mypy успешны. DB/schema/migration и deterministic decision boundaries не изменены.
  - Feature PR #92 слит merge commit `246734d2f3b700392c6682c7bcfb5d6ab1469ec5`.
    Финальный PR Quality Gate run `29659175137` и exact merge-SHA run `29659317641` успешны на
    Python 3.12/3.13; dependency audit и все обязательные jobs — `success`.
  - RM-142 соответствует Definition of Done и закрывается этим docs-only closeout; RM-143
    становится единственным `IN PROGRESS`.
- [x] RM-143 — Новая дизайн-система (`DONE`).
  - Audit/contract/matrix/plan, characterization и expected-red зафиксированы отдельными коммитами
    до application changes.
  - Один versioned immutable token root, одинаковые dark/light roles, deterministic sRGB contrast
    pairs и один semantic icon registry/fallback формируют Corteris Design System v1.
  - Canonical button/card/status/data/form components, focus/keyboard/loading states и offline
    component gallery покрыты обеими темами без business decision ownership.
  - 45 baseline local-style sites имеют exact migration decisions; итоговый guard сообщает
    `matrix=45`, `styles=43`, `violations=0`, literal colours outside theme — 0.
  - Локально: RM-143 contract `76 passed`, соседний UI contour `40 passed`, full pytest
    `2059 passed, 2 warnings`; secret scan, Ruff/format (`662 files`), mypy, frozen/build smoke и
    dependency audit успешны.
  - Feature PR #94 слит merge commit `c8d111f3db615dd3c21c231bf265bb00093c65bd`.
    PR-head run `29662950338` и exact merge-SHA run `29663124774` успешны на Python 3.12/3.13;
    full suite, dependency audit и все обязательные jobs — `success`.
  - RM-143 соответствует Definition of Done и закрывается этим docs-only closeout; RM-144
    становится единственным `IN PROGRESS`.
- [x] RM-144 — Новый каркас приложения (`DONE`).
  - Audit/contract/plan зафиксированы commit `70d06f3` до application changes;
    characterization — `a7e7b93`, expected-red contract — `fab3eae`.
  - `TenderWorkspacePage` механически извлечён в canonical page module; legacy class import имеет
    exact identity, а `MainWindow` остаётся отдельным thin compatibility wrapper.
  - Production shell содержит один `BusinessWorkflowPage`, один workflow repository/service/
    monitor/timer stack и destinations `dashboard`, `tenders`, `workflow`; child intents и legacy
    aliases сохраняют RM-142 route/context/selection semantics.
  - Monitor/page/shell lifecycle typed, bounded и idempotent; owned pool не ждёт foreign/global
    jobs, late result не публикуется после close, rapid close не создаёт deleted QObject/thread
    warning и оставляет ноль owned timers.
  - Локально: RM-144 contract `9 passed`, соседний workflow contour `79 passed`, full pytest
    `2073 passed, 2 warnings`; secret scan, Ruff/format (`666 files`), mypy, frozen/build smoke,
    design guard и dependency audit успешны.
  - Feature PR #96 слит merge commit `491b13a0b5e5dd204bf00faba09fa513c5f9de3b`.
    PR-head run `29665840955` и exact merge-SHA run `29666054057` успешны на Python 3.12/3.13;
    full suite, dependency audit и все обязательные jobs — `success`.
  - DB/schema/migration, runtime dependencies и deterministic score/recommendation/critical
    stop-factor priority не изменены. RM-144 соответствует Definition of Done и закрывается этим
    docs-only closeout; RM-145 становится единственным `IN PROGRESS`.
- [x] RM-145 — Современный dashboard (`DONE`).
  - Audit-first contract зафиксирован commit `89d8346`; characterization — `3e2ae9a`,
    expected-red contract — `3db1cbe`.
  - Один immutable шестипозиционный KPI registry владеет typed raw value, source, evidence,
    state и action. Деньги используют `Decimal`, missing — `None`, score cohort `80+` явно не
    является AI recommendation и не отменяет critical stop factors.
  - Tender/workflow sources обновляются независимо; failed source сохраняет original observation
    time и переходит `PARTIAL`/`STALE`, а один atomic ViewModel apply исключает mixed generation.
  - Closed typed filters связывают карточки с существующими tender/workflow destinations;
    repository selectors и contributor IDs обеспечивают exact stable-ID/Decimal parity.
  - Mouse/Enter/Space, disabled loading/error, source/state/freshness accessibility semantics,
    uncapped Moscow-day selection и explicit non-actionable demo покрыты тестами.
  - Локально: RM-145 contract `13 passed`, соседний contour `53 passed`, full pytest
    `2095 passed, 2 warnings`; secret scan, Ruff/format (`670 files`), mypy, frozen/build smoke,
    design guard и dependency audit успешны.
  - Feature PR #98 слит merge commit `ac8d2662911e8a0e450fcb20677f99082187793a`.
    PR-head run `29676604619` и exact merge-SHA run `29680204767` успешны на Python 3.12/3.13;
    full suite, dependency audit и все обязательные jobs — `success`.
  - DB/schema/migration, runtime dependencies и deterministic score/recommendation/critical
    stop-factor priority не изменены. RM-145 соответствует Definition of Done и закрывается этим
    docs-only closeout; RM-146 становится единственным `IN PROGRESS`.
- [x] RM-146 — Интерактивные графики (`DONE`).
  - Audit/contract/plan зафиксированы commit `aeb02e7`; characterization — `0d1584b`,
    expected-red contract — `162bd08`.
  - Один dependency-free QPainter owner `app.ui.charts` предоставляет immutable chart contracts,
    deterministic render plan, bar/line rendering и typed selection без второго theme/router/DI
    или business owner.
  - Восемь honest states, aware-time/Decimal/missing contracts, клавиатура/hover, accessible
    complete-data table и PNG/SVG/JSON/CSV exports используют одну нормализованную семантику.
  - Bounded six-series/1,000-render/10,000-data limits, resize/DPI behavior и hidden frozen smoke
    измерены; native accessibility/DPI observations переданы RM-152.
  - Локально: RM-146 focused `27 passed`, соседний contour `203 passed`, full pytest
    `2123 passed, 2 warnings`; secret scan, Ruff/format (`682 files`), required/strict mypy,
    frozen/build smoke, design guard и dependency audit успешны.
  - Feature PR #100 на head `72118c31a31f16b524c79ee83bc82a9daf7071fb` слит merge commit
    `e09af67931c3a63874e259bed08efc5ce3a14284`.
  - PR-head run `29685966343` и exact merge-SHA run `29686798140` успешны на Python 3.12/3.13;
    первый PR-head Python 3.12 job завершился native access violation без assertion, rerun того же
    SHA прошёл без изменений; exact full suites — `2123 passed, 2 warnings` на обеих версиях.
  - DB/schema/migration, runtime dependencies, KPI/tender/financial semantics и deterministic
    score/recommendation/critical stop-factor priority не изменены. RM-146 соответствует Definition
    of Done и закрывается этим docs-only closeout; RM-147 становится единственным `IN PROGRESS`.
- [x] RM-147 — Аналитика тендеров (`DONE`).
  - Audit/source-of-truth/metric/time/provenance/drill-down/export/plan contracts зафиксированы
    commit `2eea4ee`; characterization — `486dd04`, expected-red contract — `150d8b9`.
  - Один Qt-free `app.tenders.analytics` owner определяет immutable query/snapshot contracts,
    четыре truthful metrics, aware day/week/month buckets, deterministic aggregation и honest
    empty/partial/conflicted/stale/error/unavailable/too-large states вне UI.
  - Existing tender registry и collector-state repositories предоставляют bounded bulk read-only
    facts; route/page/controller переиспользует RM-146 charts и один modern-shell lifecycle без
    второго repository/router/chart/KPI/business owner.
  - Preset/custom/all-available intervals, grain/source/status/law/archive filters, exact stable-ID
    drill-down, complete textual tables и snapshot-identical atomic JSON/CSV exports сохраняют
    contributor/provenance parity и formula/control/bidi safety.
  - 10,000-record exact-data limit измерен до p95 aggregation `295.1390 ms`; 10,001 records
    fail closed как `TOO_LARGE`, без hidden sampling. Ordered/shuffled fixtures дают equal semantic
    snapshots, service query count — 0, application read-query count — 4.
  - Локально: RM-147 focused `40 passed in 7.30s`, full pytest
    `2163 passed, 2 warnings in 165.42s`; secret scan, Ruff/format (`705 files`), mypy,
    offline/migration/import/composition/build/frozen smokes, design guard и dependency audit
    успешны.
  - Feature PR #102 на head `ea84b068d437cf2e4e2e366aa94bb079938587e5` слит merge commit
    `d85cf8c99f8ee72279bbb8054942a0f4d5675ac2`.
  - PR-head run `29692568668` успешен: Python 3.12 — `4m36s`,
    `2163 passed, 2 warnings in 146.82s`; Python 3.13 — `4m40s`,
    `2163 passed, 2 warnings in 151.95s`.
  - Exact merge-SHA run `29693165086` успешен: Python 3.12 — `6m36s`,
    `2163 passed, 2 warnings in 257.05s`; Python 3.13 — `4m48s`,
    `2163 passed, 2 warnings in 150.21s`; dependency audit и все обязательные jobs — `success`.
  - DB/schema/migration, dependencies, provider/network/AI paths, financial semantics и RM-107
    score/recommendation/critical stop-factor priority не изменены. RM-147 соответствует Definition
    of Done и закрывается этим docs-only closeout; RM-148 становится единственным `IN PROGRESS`.
- [x] RM-148 — Финансовая аналитика (`DONE`).
  - Audit/numeric/currency/unit/rounding/margin/schema/parity/implementation contracts зафиксированы
    commit `b488468`; characterization — `aa8f261`, expected-red contract — `399ac71`.
  - Один Qt-free `app.financial` owner предоставляет finite Decimal, explicit currency/unit/state,
    HALF_UP boundaries, derived revenue/weighted margin, deterministic immutable snapshots,
    fingerprints и exact JSON/CSV projection без UI/repository recalculation.
  - Existing `BusinessMetricsRepository` остаётся source of truth и хранит schema v3 canonical
    fixed-point strings с RUB/margin version. Explicit v2→v3 migration имеет dry-run issues,
    byte-exact safety artifact/hash, fsync, atomic replace, all-record readback и rollback.
  - Workflow editor/table/detail/audit, Dashboard, RM-147 page/controller, RM-146 ChartSpec и
    accessible table используют один snapshot. XLSX сохраняет usable numeric cells и authoritative
    hidden `FinancialExact`; import блокирует metadata/cell/margin conflicts.
  - Backup/restore/health, corrupt recovery, concurrent writes и injected rollback сохраняют exact
    values. Missing/invalid/conflicted/unsupported currency не превращаются в zero; mixed currency
    не агрегируется; FX/network/provider/AI paths не добавлены.
  - Локально: focused `38 passed`, XLSX contour `16 passed`, full pytest
    `2209 passed, 2 warnings in 182.09s`; secret scan, Ruff/format (`722 files`), mypy,
    offline/migration/import/composition/build/frozen smokes и dependency audit успешны.
  - Feature PR #104 на head `7af94361f47660a44256751126a5871b34851202` слит merge commit
    `1116216cf00fc74dad2b870617c496242cd659c2`.
  - PR-head run `29698349596` успешен: Python 3.12 — `2209 passed, 2 warnings in 189.67s`;
    Python 3.13 — `2209 passed, 2 warnings in 142.97s`.
  - Exact merge-SHA workflow-dispatch run `29699279963` на SHA `1116216…` успешен: Python 3.12 —
    `2209 passed, 2 warnings in 131.83s`; Python 3.13 —
    `2209 passed, 2 warnings in 131.69s`; dependency audit и все обязательные jobs — `success`.
  - RM-107 score/recommendation/critical stop-factor priority, dependencies и RM-149 card scope не
    изменены. RM-148 соответствует Definition of Done и закрывается этим docs-only closeout;
    RM-149 становится единственным `IN PROGRESS`.
- [x] RM-149 — Новая карточка тендера (`DONE`).
  - Audit/source-of-truth/hierarchy/critical/action/navigation/parity/implementation decisions
    зафиксированы commit `1540d75` до production code; characterization — `cb4fd56`, expected-red —
    `f37bef9`, основной implementation — `632d5aa`.
  - Один Qt-free `app.tenders.detail` owner предоставляет typed registry/legacy identity,
    immutable snapshot/card, read-only assembler, reason codes, complete action catalog,
    deterministic fingerprint, HTTPS/source и stale-action policies.
  - Native RM-143 detail/card widgets интегрированы с exact registry and persisted-search surfaces;
    RM-147 drill-down переиспользует тот же registry owner, а legacy Dashboard не получает
    выдуманный ORM↔registry bridge. RM-148 остаётся владельцем price/currency projection.
  - Локально: focused `36 passed`, neighboring `358 passed`, full pytest
    `2245 passed, 2 warnings`; secret scan, Ruff/format (`735 files`), mypy,
    offline/migration/import/composition/build/frozen smokes и dependency audit успешны.
  - Benchmark: 10,000 card projections p50 `502.915 ms`, p95 `505.788 ms`, peak `1,608 bytes`,
    repository reads `0`; 25 post-warm-up republications имеют zero QObject/thread/timer growth.
  - Feature PR #106 на head `d7a6896b9fa2daf94e760b0fcf1ae030089adcb1` слит merge commit
    `219e7c43527ca230a61de8cdeb3f191288fc3f87`.
  - PR-head Quality Gate `29703943804` успешен: Python 3.12 job `88238135602` —
    `2245 passed, 2 warnings in 95.59s`; Python 3.13 job `88238146684` —
    `2245 passed, 2 warnings in 125.70s`.
  - Exact merge-SHA push-run `29704404132` подтверждён с
    `headSha=219e7c43527ca230a61de8cdeb3f191288fc3f87`: Python 3.12 job
    `88239262921` — `2245 passed, 2 warnings in 209.10s`; Python 3.13 job `88239263398` —
    `2245 passed, 2 warnings in 141.43s`; dependency audit и все обязательные steps — `success`.
  - Первые Python 3.12 попытки PR-head и exact runs завершились native Windows
    access/heap violation без test assertion; rerun тех же неизменных SHA прошёл полностью.
  - DB/schema/migration, dependencies, provider/network/AI paths, generic-table scope и RM-107
    score/recommendation/critical stop-factor priority не изменены. RM-149 соответствует
    Definition of Done и закрывается этим docs-only closeout; RM-150 становится единственным
    `IN PROGRESS`.
- [x] RM-150 — Современные таблицы (`DONE`).
  - Audit всех 35 pre-production product sites и решения `migrate=11`, `keep=12`, `defer=12`
    зафиксированы commit `6c79157` вместе с девятью обязательными contracts/plans до
    application-кода; characterization — `efd9402`, expected-red — `a9ba57d`.
  - Один Qt-free immutable `app.ui.tables` contract определяет stable surface/row/column/revision
    identity, typed Decimal sort/filter с identity tie-break, exact selection/action validation,
    explicit sibling states и visible-snapshot export parity.
  - Reusable Qt model/selection/state adapters и 11 approved representative migrations сохраняют
    exact target across refresh/sort/filter/recalculation, не выбирают соседнюю строку и не
    переносят repository/business logic в delegate. 12 bounded sites остаются `keep`, ещё 12 имеют
    явный `defer` owner/rationale.
  - Локально: focused `31 passed`, deterministic hash-seed contours `22 + 22 passed`, full pytest
    `2276 passed, 2 warnings`; secret scan, Ruff/format (`744 files`), mypy,
    offline/migration/import/composition/build/frozen smokes и dependency audit успешны.
  - 0/100/1k/10k benchmark не вводит arbitrary timing threshold. На 10,000 rows typed Decimal sort
    p95 улучшен с `2295.315 ms` до `90.907 ms`; missing-text filter p95 `137.449 ms` остаётся ниже
    RM-141 historical `148.005 ms`; exact raw artifacts и bounded memory trade-off сохранены.
  - Feature PR #108 на head `4f432cbe650c76994ba6c44f62685a20fb5ed555` слит merge commit
    `8d6640691ca3e0fc6a22d7e6dd2d732955e0eedd`.
  - PR-head Quality Gate `29708327405` успешен: Python 3.12 job `88248797493` —
    `2276 passed, 2 warnings in 143.14s`; Python 3.13 job `88248797488` —
    `2276 passed, 2 warnings in 113.57s`.
  - Exact merge-SHA push-run `29708473745` подтверждён с
    `headSha=8d6640691ca3e0fc6a22d7e6dd2d732955e0eedd`: Python 3.12 job `88249137810` —
    `2276 passed, 2 warnings in 116.60s`; Python 3.13 job `88249137786` —
    `2276 passed, 2 warnings in 183.21s`; dependency audit и все обязательные steps — `success`.
  - DB/schema/migration, dependencies, provider/network/AI paths и RM-107
    score/recommendation/critical stop-factor priority не изменены. Native Narrator/physical
    keyboard/DPI inspection честно `NOT_EXECUTED` и остаётся scope RM-152. RM-150 соответствует
    Definition of Done и закрывается этим docs-only closeout; RM-151 становится единственным
    `IN PROGRESS`.
- [x] RM-151 — Уведомления и фоновые операции (`DONE`).
  - Audit 30 operation groups и восемь обязательных pre-production contract/plan документов
    зафиксированы commit `c64f3c6`; characterization — `abeeecd`, expected-red — `0f91d94`,
    Qt-free core — `b719417`, owner adapters — `c233087`, bounded measurements — `627faf8`,
    acceptance — `dfa7701`.
  - Один immutable Qt-free episode contract сохраняет RM-140 queued/cancelling/timed-out semantics,
    fail-closed stale/late/terminal transitions, retry как новый attempt и confirmed cancellation.
    Allowlist-first safe feedback, bounded diagnostic correlation, schema-v1 notification adapter и
    announcement coalescer не создают второй lifecycle, scheduler, notification repository, shell
    или crash/support owner.
  - Локально: focused `42 passed`, neighboring `35 passed`, full pytest
    `2318 passed, 2 warnings`; secret scan, boundary guard, Ruff/format (`761 files`), mypy,
    offline/migration/import/composition/build/frozen smokes, benchmark и dependency audit успешны.
  - Feature PR #110 на head `dfa7701db8c669a1f095604671f615aa8c38d4b5` слит merge commit
    `7176f8542357f91b7d5283bd0b6167efcc63982e`.
  - PR-head Quality Gate `29710971738` успешен: Python 3.12 job `88254690838`, Python 3.13 job
    `88254690835`. Exact merge-SHA push-run `29711141067` подтверждён с
    `headSha=7176f8542357f91b7d5283bd0b6167efcc63982e`: Python 3.12 job `88255104161`, Python
    3.13 job `88255104196`; full suite, dependency audit и все обязательные steps — `success`.
  - DB/schema/migration, dependencies, notification storage schema, provider/network/AI paths и
    RM-107 score/recommendation/critical stop-factor priority не изменены. Native Narrator,
    physical keyboard/high-contrast/DPI inspection остаются честно `NOT_EXECUTED` и переходят в
    активный RM-152. RM-151 соответствует Definition of Done и закрывается этим docs-only closeout.
- [x] RM-152 — DPI и accessibility (`DONE`).
  - Audit/contracts, characterization and expected-red/green packages established one shell focus
    chain, table Tab-release, safe focus restore, accessible metadata, geometry clamp, contrast
    inventory and a fail-closed native evidence validator without a second UI/business owner.
  - Dark-theme white fallback strips and Russian safe-feedback mojibake were reproduced, fixed and
    guarded. Exact frozen artifact native checks covered representative 1920x1080 100/125/150%,
    Narrator and High Contrast states; Windows scale and High Contrast were restored afterwards.
  - Native evidence remains truthful: `0 PASS`, `4 BLOCKED`, `29 NOT_EXECUTED`. Owner decision
    `RM152-OWNER-EXCEPTIONS-2026-07-20` names all 33 cells with exact environment, reason,
    residual risk and retained status; strict validation rejects malformed or unnamed exceptions.
  - Locally: owner-exception/static `20 passed`, stop-factor contour `8 passed`, full pytest
    `2345 passed, 2 warnings in 197.56s`; secret scan, strict RM-152 gate, Ruff/format (`772 files`),
    mypy, offline/migration/import/composition/build/frozen smokes and dependency audit passed.
  - Final closeout EXE SHA-256 is
    `5BED2D3F30AE6917F911800FBB85D7679BFBA6CEBB76F6F98F6B73376EBC2719`; isolated frozen
    self-test passed all nine checks.
  - Feature PR #112 on head `ae70c0ae5ee5fff0a1bcf374361d82d80bfb329a` merged as
    `5f20df74b89fcf6d67c7c79faa2e8cceca4b206b`.
  - PR-head Quality Gate `29776619427` passed: Python 3.12 job `88467423008`, Python 3.13 job
    `88467423174`. Exact merge-SHA push-run `29777125490` confirmed
    `headSha=5f20df74b89fcf6d67c7c79faa2e8cceca4b206b`: Python 3.12 job `88469119363`, Python
    3.13 job `88469119432`; full suite, dependency audit and all required steps are `success`.
  - DB/schema/migration, dependencies, provider/network/AI/keyring/domain paths and RM-107
    score/recommendation/critical stop-factor priority are unchanged. RM-152 satisfies the
    Definition of Done and is closed by this docs-only closeout.
- [x] RM-153 — Производительность UI (`DONE`).
  - Audit/contract/plan, characterization, expected-red and deterministic benchmark evidence were
    committed before and alongside production optimization. The versioned benchmark covers shell
    construction/first paint/shutdown, routing, dashboard, theme, table, chart and resources.
  - One monotonic theme epoch scopes repolish to shell chrome and the active page; stale hidden
    pages synchronize through the existing route owner before exposure. No second shell/router,
    theme/table/chart/lifecycle owner, timer, thread, persistence, telemetry or dependency exists.
  - Post p95 versus baseline: shell construction `1114.509 -> 627.888 ms` (-43.7%), first paint
    `232.923 -> 157.609 ms` (-32.3%), page switch `47.737 -> 36.344 ms` (-23.9%), dashboard update
    `59.853 -> 31.753 ms` (-47.0%), theme switch `742.422 -> 188.245 ms` (-74.6%), chart update
    `43.424 -> 36.106 ms` (-16.9%); table filter +2.8% remains inside the 155 ms guard.
  - Fresh 25-cycle resource evidence has non-positive QObject/QThread/QTimer/active-timer/Python
    thread growth. Eight-page dark/light inspection and owner native confirmation passed page
    responsiveness, no white strips after theme switching and normal native close.
  - Locally: focused/neighboring `39 passed`, final focused `13 passed`, full pytest
    `2354 passed, 2 warnings in 193.39s`; secret scan, design audit, Ruff/format (`775 files`), mypy,
    workflow smokes and dependency audit passed.
  - Feature PR #114 on head `ecff610e77bebcbec316dc5db1888ec1894dcfe9` merged as
    `1e8ddf02177a460e14151c7482d5e1cd7dc8e5ad`.
  - PR-head Quality Gate `29785396286` passed: Python 3.12 job `88495726494`, Python 3.13 job
    `88495726570`. Exact merge-SHA push-run `29787372667` confirmed
    `headSha=1e8ddf02177a460e14151c7482d5e1cd7dc8e5ad`: Python 3.12 job `88501750635`, Python
    3.13 job `88501750646`; full suite, dependency audit and all required steps are `success`.
  - DB/schema/migration, dependencies, provider/network/AI/keyring/domain paths and RM-107
    score/recommendation/critical stop-factor priority are unchanged. RM-153 satisfies the
    Definition of Done and is closed by this docs-only closeout.
- [x] RM-154 — Визуальное тестирование (`DONE`).
  - Audit/decision/contract/expected-red packages define one deterministic visual QA owner without
    duplicating shell/router/theme/chart/table/dialog or business logic.
  - Fourteen normalized PNGs cover seven representative dark/light pairs. Canonical Windows
    Python 3.12 renderer fingerprint is
    `f1cd92373456028fd9360b3a032ef9b8d5784dc90d00abad4080d404db0dba56`; strict RGB policy has
    zero tolerance and no masks. Repeatability and deliberate token/layout mutation gates pass.
  - Baseline update is canonical, explicit and review-only. Fixtures are synthetic/offline;
    privacy/path/hash/size checks pass and visual artifacts are excluded from frozen packaging.
  - Locally: focused `30 passed`, full pytest `2378 passed, 2 warnings in 199.01s`, RM-153 guards
    `9 passed`; secret/design/UI/security/dependency gates, real one-file build and nine-check
    frozen self-test pass. RM-152 native statuses remain `0 PASS`, `4 BLOCKED`, `29 NOT_EXECUTED`.
  - Feature PR #116 head `109f084aaf84cd907b849d17635bb7cfad1d97ab` merged as
    `40f0e327d0d485b93e93f39bab1d838e584b8914`.
  - PR-head Quality Gate `29822184296` passed: Python 3.12 job `88607135547`, Python 3.13 job
    `88607135493`. Exact merge-SHA run `29823579968` confirmed that merge: Python 3.12 job
    `88611629793`, Python 3.13 job `88611629760`; full suite and dependency audit pass on both,
    and strict canonical comparison is 14/14 `PASS` on Python 3.12.
  - DB/schema/migration, production dependencies, RM-107 deterministic decision priority and
    RM-152/RM-153 guards are unchanged. RM-154 satisfies the Definition of Done and is closed by
    this docs-only closeout.
- [x] RM-155 — Завершение редизайна (`DONE`).
  - Audit-first inventory классифицировал 32 compatibility candidates: 9 `REMOVE`, 2 `MIGRATE`,
    21 `KEEP`, 0 `DEPRECATE`, 0 `BLOCKED`; каждый имеет consumer/history/runtime/settings/frozen/
    public evidence, replacement, owner и rollback.
  - Controlled cleanup удалил только obsolete `app.ui.main_window`, same-object `quotes_page`/
    `estimates_page`, их bootstrap fallbacks и unused search shim. Canonical shell, router, tender/
    workflow pages, controllers, repositories, routes, settings, public/data adapters остаются
    единственными production owners.
  - J01–J16 и RM-142–RM-154 cross-stage gates прошли. RM-107 score/recommendation/critical
    stop-factor priority, Decimal/identity/export/data contracts не изменены.
  - Локально: final full pytest `2411 passed, 2 warnings in 207.24s`, neighboring RM-127–154
    `840 passed`; Ruff/format (`794 files`), mypy, secret, offline, migration, composition,
    build/frozen and dependency gates успешны. Fresh RM-153 performance/resource budgets прошли.
  - Actual one-file EXE SHA-256
    `044B35A3D8D73132A603073FBB0F8456010950B19CB5696C2EFBB8D7BC41F7A0`; все девять isolated
    frozen self-test checks прошли. RM-152 native evidence остаётся `0 PASS`, `4 BLOCKED`,
    `29 NOT_EXECUTED`.
  - Feature PR #118 head `c741ba6a39750436fa34ffc2237bd1c264466745` слит merge commit
    `63a85b4cff5e2de5b53e4fad6dcfb091371200bf`. PR-head run `29832379070` и exact merge-SHA run
    `29845412052` успешны на Python 3.12/3.13; canonical Python 3.12 visual comparison —
    `14/14 PASS`.
  - `UI-141-017` закрыт; RM-141–RM-155 redesign sequence завершена. No schema/data/settings
    migration required; rollback — revert feature merge без user-data downgrade.

# RM-156–RM-168 — оценка контрагентов
- [ ] RM-156 — Модель контрагента (активный этап).
- [ ] RM-157 — Поиск по ИНН.
- [ ] RM-158 — Архитектура источников.
- [ ] RM-159 — Регистрационные сведения.
- [ ] RM-160 — Финансово-хозяйственная деятельность.
- [ ] RM-161 — Арбитражные дела.
- [ ] RM-162 — Исполнительные производства.
- [ ] RM-163 — Государственные контракты.
- [ ] RM-164 — Связи и аффилированность.
- [ ] RM-165 — Оценка надёжности.
- [ ] RM-166 — Графики контрагента.
- [ ] RM-167 — Замечания и ручное заключение.
- [ ] RM-168 — Отчёт о контрагенте.

# RM-169–RM-178 — AI-анализ договоров
- [ ] RM-169 — Вкладка Договоры.
- [ ] RM-170 — Безопасный импорт.
- [ ] RM-171 — Структура договора.
- [ ] RM-172 — Чтение и навигация.
- [ ] RM-173 — Детерминированная проверка.
- [ ] RM-174 — AI-анализ.
- [ ] RM-175 — Сравнение версий.
- [ ] RM-176 — Оценка рисков.
- [ ] RM-177 — Протокол разногласий.
- [ ] RM-178 — Стабилизация модуля.

# RM-179–RM-188 — подписки и коммерческая модель
- [ ] RM-179 — Тарифы.
- [ ] RM-180 — Entitlements.
- [ ] RM-181 — Сервер лицензирования.
- [ ] RM-182 — Привязка аккаунта и устройства.
- [ ] RM-183 — Блокировка платных функций.
- [ ] RM-184 — Управление подпиской.
- [ ] RM-185 — Оплата через сайт.
- [ ] RM-186 — Webhook и синхронизация.
- [ ] RM-187 — Offline grace period.
- [ ] RM-188 — Аудит коммерческой модели.

# RM-189–RM-200 — защита и выпуск 2.0
- [ ] RM-189 — Модель угроз.
- [ ] RM-190 — Удаление секретов из клиента.
- [ ] RM-191 — Nuitka и нативные модули.
- [ ] RM-192 — Перенос критической логики на сервер.
- [ ] RM-193 — Authenticode.
- [ ] RM-194 — Подписанные обновления.
- [ ] RM-195 — Целостность и anti-tamper.
- [ ] RM-196 — Reverse-engineering аудит.
- [ ] RM-197 — Архитектурный аудит.
- [ ] RM-198 — Regression/UI/security прогон.
- [ ] RM-199 — Финальная сборка и руководства.
- [ ] RM-200 — Релиз CorterisTenderAI 2.0.
