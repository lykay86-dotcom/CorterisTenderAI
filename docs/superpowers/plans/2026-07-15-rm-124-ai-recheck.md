# RM-124 — implementation plan

Baseline: `52c1f5ce72c13f945817225ba2e1e35e9bf400f0`.

Branch: `feat/rm-124-ai-recheck`.

## 1. Audit commit

- Зафиксировать `docs/RM-124_AUDIT.md`, `docs/RM-124_REQUIREMENTS.md` и этот plan.
- Commit: `docs(rm-124): audit AI recheck semantics`.

## 2. RED contract

- Создать `tests/test_ai_recheck.py` для immutable types, statuses, comparability, digests,
  locally verified exact-key deltas, deterministic ordering и security boundary.
- Расширить service/orchestrator/repository/UI/export/runtime/RM-107 tests из ТЗ.
- Запустить target и сохранить ожидаемый RED, вызванный отсутствием RM-124 API.
- Commit tests: `test(rm-124): define AI recheck contract`.

## 3. Domain и application implementation

- Добавить pure `app/core/ai/recheck.py` и публичные exports.
- Выделить private prepared-context/postprocess path в existing service без изменения normal cache
  semantics.
- Добавить отдельные service/orchestrator `recheck`; exact baseline читать до одного analyzer
  call, current failure не заменять baseline.
- Сохранить existing repository/table/payload/provider versions.

## 4. UI/export implementation

- Добавить button/signal/confirmation/in-flight state/result rendering в existing dialog.
- Добавить background worker и per-registry guard в existing controller.
- Передать optional assessment existing exporter; сохранить прежний export path без recheck.
- Проверить escaping и отсутствие private/raw data.

## 5. Acceptance и feature publication

- Запустить target eight-file contour, затем full pytest.
- Запустить Ruff check/format, mypy, secret scan, pip-audit и diff-check.
- Подтвердить static architecture/version/RM-107 invariants.
- Записать exact результаты в `docs/RM-124_REQUIREMENTS.md` и roadmap acceptance section.
- Implementation commit: `feat(rm-124): add explainable AI recheck`.
- Acceptance commit: `docs(rm-124): record feature acceptance`.
- Push и PR title: `feat(rm-124): add explainable AI recheck`.

## 6. Post-merge closeout

- После merge и успешного Windows Quality Gate создать `docs/rm-124-completion` от fresh main.
- Изменить только `docs/ROADMAP.md`, `docs/STATUS.md`, `docs/ROADMAP_HISTORY.md`.
- Commit: `docs(rm-124): complete AI recheck stage`.
- После docs-only merge и финального Quality Gate отметить RM-124 `DONE`, RM-125 — единственным
  `IN PROGRESS`.
